{
  "version": 1,
  "issue": "",
  "workflow": "",
  "codemap": "rulebook.md — the session rulebook the issue orders compressed; test-repo.sh keys cases on its material-terms list, its Issue Mode numbered step and the phrase 'small or obvious'\nGEMINI.md — must stay byte-identical to rulebook.md; today a symlink to it, so it follows rulebook.md unless the change breaks the link (a cmp case in test-repo.sh guards the identity)\nagents/implementer.md — agent page to compress; test-repo.sh checks it names the codemap\nagents/planner.md — agent page to compress; test-repo.sh holds it as the sole owner of the phrase 'chain depth' among agent pages and shipped skills\nagents/researcher.md — agent page to compress; test-repo.sh keys cases on its phrases (per-criterion go-red case, 'production change', the breaks/unbreakable templates, 'says so and why')\nagents/reviewer.md — agent page to compress, the largest; test-repo.sh checks it points at the mutation standard without restating it\nagents/test-author.md — agent page to compress\nskills/agent-brief/SKILL.md — skill page to compress; test-repo.sh keys on '## The mutation standard', sole ownership of 'counts as tested', 'push the commit', 'already committed', 'backlog.mjs'\nskills/argus/SKILL.md — skill page to compress\nskills/grill/SKILL.md — skill page to compress\nskills/retro/SKILL.md — skill page to compress\n.claude/rules/agents.md — rules page to compress; deliberately outside test-repo.sh's cross-file duplicate checks, and a case requires it to point at .claude/rules/authoring.md\n.claude/rules/authoring.md — rules page to compress; test-repo.sh parses its table rows and its paths: frontmatter (skills/** and agents/**)\nworkflows/agile-loop.js — the only workflow artifact; its dispatched prompt and schema-description strings are the workflow's rule text the issue calls a workflow page, and several test-repo.sh cases grep this file\ntest-repo.sh — re-key every case whose grep keys on wording the compression changed so it still turns red when its rule is removed; delete no case",
  "increments": [
    {
      "id": "agent-pages",
      "title": "Compress the five agent pages",
      "goal": "Every page under agents/ says all it said in fewer words, and the suite still guards it.",
      "criteria": [
        "Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words.",
        "Everything the old text of these pages required is still required, and everything it forbade is still forbidden.",
        "Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.",
        "Each page's role, audience, file location and English are unchanged.",
        "Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.",
        "bash test.sh exits 0."
      ],
      "depth": "full",
      "status": "done",
      "note": "Review accepted at round 1 after one direct-fix correction (a dropped prohibition was reinserted). Worked on the issue branch itself, so nothing to merge. Pages now at 996/2099/1463/2823/1072 words, each under its old count; bash test.sh exit 0.",
      "branch": "",
      "steps": [],
      "attempts": [
        {
          "closedAs": "done",
          "at": "2026-08-16T21:15:26.962Z",
          "steps": [
            {
              "label": "research:agent-pages.0",
              "at": "2026-08-16T20:38:39.113Z",
              "return": {
                "needsTests": true,
                "plan": "WHAT GETS BUILT. Rewrite the five pages under agents/ — implementer.md, planner.md, researcher.md, reviewer.md, test-author.md — so each says everything it says now in fewer words: every requirement and prohibition survives, each rule stands once, sentences are short and imperative, and no reason runs past one sentence. Current word counts (wc -w, whole file): implementer 1095, planner 2203, researcher 1561, reviewer 2909, test-author 1134; each must end strictly below its number. The implementer edits ONLY the five pages. It never edits test-repo.sh: the five new word-cap cases there are the test-author's, and every existing case stays byte-identical because the plan keeps every phrase those cases grep for. Out of scope (later increments): rulebook.md, GEMINI.md, skills/**, .claude/rules/**, workflows/agile-loop.js, hooks/**.\n\nMETHOD. test-repo.sh holds dozens of cases that grep these pages for exact phrases, forbidden old wordings, per-line co-occurrences and paragraph structure. The design decision of this plan: keep every one of those keys verbatim and compress only the prose around them. Then no case is re-keyed, the re-key criterion holds vacuously, and the compression cannot silently unguard a rule. What is compressible: frontmatter descriptions (they count toward wc -w), connective and explanatory prose, and restatements a page makes twice. The keyed sentences are mostly the rules themselves in their measured winning form (prohibition + quoted excuse + one-sentence price, or a template) — .claude/rules/authoring.md is the form standard and loads when you open agents/* — so never rewrite a keyed rule out of that form. If a keyed sentence is the only thing left to shorten, leave it: a page a few words over its ideal still meets every criterion, and editing test-repo.sh is not yours. Work page by page; after each page run bash test-repo.sh (the inner loop this plan names — the judge stays bash test.sh) and repair any case you turned red before moving on. Before any edit, run the test-author's five word-cap cases and confirm each fails because its page still stands at its old count.\n\nTHE FIXED POINTS. Harvest the full literal key list from test-repo.sh before editing each page — every quoted grep pattern aimed at agents/*.md in these line ranges is a fixed point, and each case's comment names its Break: 185-191, 244-252, 327-350, 364-398, 449-470, 472-548, 550-584, 686-722, 724-764, 766-794, 2402-2427, 2429-2514, 2516-2602, 2604-2667, 2715-2781, 2783-2813, 3046-3067, 3252-3262, 3449-3676, 3678-4015. Presence patterns must survive character-for-character (many are matched against the page collapsed to one line with tr, so they may wrap across a line break but not change); absence patterns must never be reintroduced.\n\nPage-wide word rules, all five pages: forbidden anywhere — 'rulebook'; 'handoff' in any spelling; 'argus', 'OTEL_', 'UROBOROS_OBS', 'collector'; 'probe' (reviewer.md alone owns it); 'chain depth' (planner.md alone); 'counts as tested' and 'dependency footprint' (the shared brief alone); 'exemption clause', 'rationalisation', 'occasion' (.claude/rules/authoring.md alone); the file names researcher.md, test-author.md, implementer.md, reviewer.md, planner.md, backlog.md except directly after a '/'. Required somewhere on every page: 'backlog.json' and 'rulings'. On implementer.md and test-author.md every physical line containing 'push' (any case, description included) must contain 'where its prompt asks for a push'. No sentence anywhere may describe deleting a branch from the remote.\n\nPer-page constraints beyond the harvested greps. implementer.md: keep 'Never open `issue.md`' inside step 1 of '## How you work' — hooks/read-barrier.mjs cites it and test-repo.sh ~4010 pins the pairing; keep the collapsed-line phrases 'need a material decision' and 'leave a material decision open'; the already-red-failure rule is currently stated twice (end of step 4's aftermath paragraph and the `commands` bullet) — state it once, in the `commands` bullet. planner.md: keep the increment template block (it must still collapse to 'goal what it delivers', 'criteria its own acceptance criteria, one string each', 'depth its chain depth'), the three bold '**Where the review accepted it**'/'**Where the review did not accept it**'/'**Where the merge conflicts**' bullets, and the '<path> — <why the issue has to change it>' and '<criterion> → <increment id>' templates. researcher.md: keep the breaks/unbreakable templates, 'every criterion stands in exactly one of the two lists', 'a plan that needs no tests puts every criterion in unbreakable', the What and How case templates, 'at least one case', 'broken or removed', 'production change', 'mutation standard', 'suite doc', 'says so and why', and 'the implementer never sees it' on the testPlan bullet (read-barrier citation). test-author.md: keep 'CLAUDE.md', the cases-field template, 'take no other field of it' in step 1 (read-barrier citation), and one sentence saying why the frontmatter names a smaller model tier (.claude/rules/agents.md requires a page that names a tier to say why). reviewer.md — structure is load-bearing, not only wording: (1) keep the literal numbered lines '3. **The tests against the intent.**' and '4. **Beyond the criteria.**' — awk anchors — and check 3's own text must contain 'mutation standard' and 'no break'; (2) keep exactly one '## ' heading containing 'probe', exactly one containing 'break', one containing 'correction round', one containing 'reproduction', and '## What you record'; (3) the probe, break and correction-round sections are checked per blank-line-delimited paragraph — each rule's term-set must land in a paragraph of its own section, and in the break section in EXACTLY ONE paragraph — so keep the one-rule-per-paragraph layout and compress inside paragraphs, never merging or splitting one without re-running the suite; (4) exactly one paragraph of the whole body may contain both 'pair of eyes' and 'break' — the opening paragraph, keep it so; (5) 'backlog.json' must share a physical line with 'never read'/'not read'/'without reading' (the heading '## You never read `backlog.json`' satisfies it — keep that heading, and keep 'the helper's reading subcommands are not yours' nearby, both cited by hooks/read-barrier.mjs) and 'backlog.json' must share a physical line with 'diff' (check 2's 'Where a changed file is `backlog.json`, it is not part of the diff you judge' — mind reflow); (6) in the '- **`summary`**' bullet, 'probe' must appear within the bullet line plus three following lines, and 'diff' and 'break' within the bullet line plus four; (7) any sentence matching nothing…agent/role…produced must contain 'break' in that same sentence — the description's 'Apart from those commands and the plan's per-criterion breaks, it is given nothing else any agent produced' qualifies; keep the qualifier if the sentence stays.\n\nREJECTED. Re-keying cases to newly shortened rule sentences — dozens of literal keys, each a red-suite risk, for savings the word criterion does not need; the unkeyed prose is plenty. Merging reviewer paragraphs for brevity — the exactly-one-paragraph counts go red. A CLAUDE.md suite doc for test-repo.sh — a repo-root CLAUDE.md loads as project memory into every session and subagent, which this repository's architecture forbids.\n\nDONE WHEN the five word-cap cases pass, nothing else in test-repo.sh turned red, and bash test.sh exits 0.",
                "moduleMap": "agents/implementer.md — the implementer's role page: brief-reading and tests-first workflow, direct-fix rounds, unplanned increments, boundaries, return fields; 1095 words to shrink — entry points: frontmatter description; '## How you work', '## Direct-fix rounds', '## An increment nobody planned', '## Boundaries', '## What you record'.\nagents/planner.md — the planner's role page: what an increment is, the codemap, chain depth, first and later calls, prohibitions, recorder subcommands, increment template, return; 2203 words — entry points: frontmatter description; '## What an increment is', '## The codemap', '## How deep the chain goes', '## Your brief', '## What you may not do', '## What you write', '## What you return'.\nagents/researcher.md — the researcher's role page: research discipline, recorded fields, the test-plan contract, correction rounds, boundaries; 1561 words — entry points: frontmatter description; '## What you record', '## The test plan', '## Correction rounds', '## Boundaries'.\nagents/reviewer.md — the reviewer's role page, the largest: checks 1-4, correction rounds, reproduction rule, run-state ban, no-write rule, probe licence, break execution, return fields; 2909 words; its headings and blank-line paragraph boundaries are load-bearing for awk-scoped suite cases — entry points: frontmatter description; '## What you check', '## A correction round', '## The reproduction rule', '## You never read `backlog.json`', '## You touch no code', '## The probe', '## The break you run and the criterion you read', '## What you record'.\nagents/test-author.md — the test-author's role page: write-the-planned-cases workflow, prove-the-failure rule, suite-doc duty, boundaries, cases field; 1134 words — entry points: frontmatter description; '## How you work', '## Boundaries', '## What you record'.\ntest-repo.sh — the repository-facts suite; dozens of cases grep the five pages (line ranges in the plan); gains five word-cap cases from the test-author and needs no other edit — entry point: bash test-repo.sh, ok/no helpers, echo '===' sections.\nCodemap correction: none — the codemap is right for this increment; add no file. One constraint it cannot show: hooks/read-barrier.mjs (untouched) quotes four sentences of these pages — implementer's 'Never open `issue.md`', researcher's 'the implementer never sees it', reviewer's '## You never read `backlog.json`' heading with 'the helper's reading subcommands are not yours', test-author's 'take no other field of it' — and test-repo.sh ~4010 pins the implementer pairing, so those sentences are fixed points of the compression.",
                "environment": "bash test.sh — from the repository root; the closed judging command. Runs test-repo.sh, test-worktree.sh, node --test on skills/agent-brief/assets/backlog.test.mjs, hooks/backlog-changed.test.mjs and hooks/read-barrier.test.mjs, and the npm test scripts of tools/argus, tools/argus-ui and tools/log-parser. Prerequisites: bash, node and npm on PATH; no install step — test.sh's own comments say the npm suites are zero-dependency. Unrun by this step.\nbash test-repo.sh — the single-file command for the five new cases; the test-author runs it once to prove them red, and the implementer runs it as the per-page inner loop the plan names. Unrun by this step.\nwc -w < agents/<page>.md — how each word cap is measured when the cases are written and when a page is judged against its cap.\nThere is no linter and no formatter for these files.",
                "testPlan": "WHETHER. Tests, yes — but only five new cases. Every rule these pages carry is already pinned by existing test-repo.sh cases, the plan keeps their keys verbatim, and they go on guarding unchanged. The one criterion nothing yet guards is 'shorter than it was', and it gets one case per page.\n\nWHAT — all five cases prove the criterion 'Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words':\n1. agents/implementer.md — the file at HEAD → wc -w < agents/implementer.md prints a number strictly below 1095 → break: append the page's pre-compression body (git show <merge-base>:agents/implementer.md) below the current body so the count reaches 1095 again.\n2. agents/planner.md — same shape → strictly below 2203 — break: same regrowth on planner.md.\n3. agents/researcher.md — same shape → strictly below 1561 — break: same regrowth on researcher.md.\n4. agents/reviewer.md — same shape → strictly below 2909 — break: same regrowth on reviewer.md.\n5. agents/test-author.md — same shape → strictly below 1134 — break: same regrowth on test-author.md.\nEdge: equality is failure — the pages stand at exactly these counts today, so assert -lt <old count>; that is also what makes each case red before the compression lands, which is the proof it works. Measure with wc -w < path (stdin form, so no filename lands in the output). Verify each number with that command before writing; where a measured count differs, the measured one wins — the criterion is 'shorter than it was'.\n\nCriteria with no new case, and why — each omission is a decision:\n- 'Everything the old text of these pages required is still required, and everything it forbade is still forbidden' — carried by the existing test-repo.sh cases, which turn red when a guarded rule is removed; the reviewer's named break for this criterion exercises one of them.\n- 'Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence' — deliberately untested: no computable predicate tells one rule worded twice from two rules, or a common word from a rare one; the reviewer reads each page against its merge-base text.\n- 'Each page's role, audience, file location and English are unchanged' — the file-location half is carried by the existing plugin.json-agrees-with-agents/ case; role, audience and English are the reviewer's reading.\n- 'Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted' — the plan changes no keyed wording, so no case is re-keyed and the existing cases' own Breaks still turn them red; whether a case was deleted is read off the test-repo.sh diff, which must hold nothing but the five added cases.\n- 'bash test.sh exits 0' — the judging command itself; running it is the test.\n\nHOW — every new case: integration (repository-fact check) — test-repo.sh at the repository root — plain bash, the file's own ok/no helpers — the command that runs just this file: bash test-repo.sh.\nSuite conventions: test-repo.sh has no CLAUDE.md suite doc, deliberately — a CLAUDE.md beside it would sit at the repository root and load as project memory into every session and subagent, which this repository's architecture forbids (its rulebook reaches sessions through a hook for exactly that reason). Do NOT create one; this paragraph is the doc statement my page requires when the doc is missing. The file documents itself: a section opens with echo \"=== <what the section pins>\", each case is preceded by a comment naming the rule it pins and the Break that turns it red, and ok/no count passes and failures. Follow that style: append one new section at the end of the file — e.g. echo \"=== the agent pages stay at their compressed size\" — one comment plus if/ok/no block per page, the section comment citing docs/issues/2026-08-16-compress-the-rules and each case comment naming its Break (the regrowth above).\nProve the failures: run bash test-repo.sh once after writing. The five new cases must be the only new FAILs, each red because its page still stands at its old count — print the measured count in the no-message so the failure names itself. Quote the five FAIL lines in your return. Anything else red in the file goes in openQuestions and stays untouched.\n\nWHAT COUNTS AS DONE. Exactly one command, run from the repository root: bash test.sh. The list is closed.\n\nWHAT IS ALREADY RED. This list is unrun — no decision in this plan turned on a fact only a run could settle, so the first runs are the test-author's failure proof and the implementer's inner loop. Expected shape once the tests land and before the compression: bash test-repo.sh red on exactly the five new cases.",
                "checks": [
                  "bash test.sh"
                ],
                "breaks": [
                  "Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words — append one page's pre-compression body (git show <merge-base>:agents/planner.md) below its current body so its wc -w count reaches its old count (2203 for planner.md) again; that page's word-cap case in test-repo.sh fails and bash test.sh exits 1.",
                  "Everything the old text of these pages required is still required, and everything it forbade is still forbidden — delete from agents/implementer.md the sentence beginning 'Never open `issue.md`' in step 1 of '## How you work'; test-repo.sh's case \"the implementer's never-read-issue rule is a priced prohibition quoting its excuse\" fails and bash test.sh exits 1.",
                  "Each page's role, audience, file location and English are unchanged — rename agents/reviewer.md to agents/review.md; test-repo.sh's case 'plugin.json declares every page in agents/ and nothing that is not there' fails and bash test.sh exits 1 (the role, audience and English halves are judged by reading, as the test plan says).",
                  "Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted — in test-repo.sh's agents/implementer.md codemap case (~line 466), change grep -qi 'codemap' to grep -qi 'codemapp', a key no page wording satisfies; that case fails and bash test.sh exits 1 (the no-case-deleted half is judged by reading the test-repo.sh diff, as the test plan says).",
                  "bash test.sh exits 0 — delete the breaks/unbreakable bullet from '## What you record' of agents/researcher.md; test-repo.sh's case 'agents/researcher.md gives both breaks and unbreakable their own template' fails and bash test.sh exits 1."
                ],
                "unbreakable": [
                  "Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence — no test can catch it: whether two differently worded sentences state one rule, whether a word is common, and whether a passage is self-justification are semantic judgements no grep, count or command computes; the reviewer judges it by reading each compressed page against its merge-base text."
                ],
                "questions": [],
                "rulings": [
                  "Keep every grep key test-repo.sh already holds verbatim in the compressed pages, so no existing case is re-keyed and the re-key criterion holds vacuously, because re-keying dozens of literal keys multiplies suite-breakage risk for words the shorter-than-it-was criterion does not need; wrong costs a correction round re-keying the few cases a needed rewording touches.",
                  "Guard the shorter-in-words criterion with five new test-repo.sh cases capping each page strictly below its pre-compression wc -w count (1095, 2203, 1561, 2909, 1134), because that criterion needs a case that goes red when a page regrows and test-repo.sh owns repository facts; wrong costs deleting or re-capping five one-line cases.",
                  "Create no CLAUDE.md suite doc for test-repo.sh, because a repo-root CLAUDE.md loads as project memory into every session and subagent, which this repository's architecture forbids — the suite's conventions live in its own comments; wrong costs writing one doc later.",
                  "Frontmatter descriptions count toward the word measure and may be compressed under the same keyed constraints, because wc -w measures the file whole and the criteria address each page whole; wrong costs restoring a description sentence."
                ],
                "summary": "Plan for compressing the five agent pages: keep every phrase test-repo.sh greps for verbatim and cut only the unkeyed prose, so no existing case needs re-keying; the test-author adds five word-cap cases (pages stand at 1095/2203/1561/2909/1134 words and must end strictly below), and bash test.sh alone judges the work."
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 1 is yours:\n  Compress the five agent pages — Every page under agents/ says all it said in fewer words, and the suite still guards it.\nWhat it has to satisfy:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden.\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.\n  - Each page's role, audience, file location and English are unchanged.\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the workflow's dispatched rule text; Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/2026-08-16-compress-the-rules/backlog.json`, before you begin.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages research:agent-pages.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages research:agent-pages.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `breaks`\n  - `unbreakable`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "tests:agent-pages.0",
              "at": "2026-08-16T20:40:26.809Z",
              "return": {
                "cases": [
                  {
                    "case": "agents/implementer.md — the file at HEAD → wc -w < agents/implementer.md prints a number strictly below 1095",
                    "file": "test-repo.sh",
                    "testName": "agents/implementer.md is shorter than its pre-compression 1095 words ($implementer_words)",
                    "want": "wc -w < agents/implementer.md is strictly less than 1095",
                    "got": "FAIL — agents/implementer.md is not shorter than its pre-compression 1095 words (1095)"
                  },
                  {
                    "case": "agents/planner.md — same shape — strictly below 2203",
                    "file": "test-repo.sh",
                    "testName": "agents/planner.md is shorter than its pre-compression 2203 words ($planner_words)",
                    "want": "wc -w < agents/planner.md is strictly less than 2203",
                    "got": "FAIL — agents/planner.md is not shorter than its pre-compression 2203 words (2203)"
                  },
                  {
                    "case": "agents/researcher.md — same shape — strictly below 1561",
                    "file": "test-repo.sh",
                    "testName": "agents/researcher.md is shorter than its pre-compression 1561 words ($researcher_words)",
                    "want": "wc -w < agents/researcher.md is strictly less than 1561",
                    "got": "FAIL — agents/researcher.md is not shorter than its pre-compression 1561 words (1561)"
                  },
                  {
                    "case": "agents/reviewer.md — same shape — strictly below 2909",
                    "file": "test-repo.sh",
                    "testName": "agents/reviewer.md is shorter than its pre-compression 2909 words ($reviewer_words)",
                    "want": "wc -w < agents/reviewer.md is strictly less than 2909",
                    "got": "FAIL — agents/reviewer.md is not shorter than its pre-compression 2909 words (2909)"
                  },
                  {
                    "case": "agents/test-author.md — same shape — strictly below 1134",
                    "file": "test-repo.sh",
                    "testName": "agents/test-author.md is shorter than its pre-compression 1134 words ($test_author_words)",
                    "want": "wc -w < agents/test-author.md is strictly less than 1134",
                    "got": "FAIL — agents/test-author.md is not shorter than its pre-compression 1134 words (1134)"
                  },
                  {
                    "case": "Everything the old text of these pages required is still required, and everything it forbade is still forbidden",
                    "file": "",
                    "testName": "",
                    "want": "carried by existing test-repo.sh cases that already turn red when a guarded rule is removed",
                    "got": "no new case: plan marks this covered by existing cases, none of which key on wording this compression changes"
                  },
                  {
                    "case": "Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence",
                    "file": "",
                    "testName": "",
                    "want": "no computable predicate distinguishes a rule worded twice from two rules, or a common word from a rare one",
                    "got": "no new case: plan marks this deliberately untested, left to the reviewer's reading against the merge-base text"
                  },
                  {
                    "case": "Each page's role, audience, file location and English are unchanged",
                    "file": "",
                    "testName": "",
                    "want": "file-location half carried by the existing plugin.json-agrees-with-agents/ case; role, audience and English are reviewer's reading",
                    "got": "no new case: plan marks this covered by an existing case plus reviewer judgement"
                  },
                  {
                    "case": "Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted",
                    "file": "",
                    "testName": "",
                    "want": "the plan changes no keyed wording, so no case is re-keyed; verified by the test-repo.sh diff holding only the five added cases",
                    "got": "no new case: nothing to re-key per the test plan"
                  },
                  {
                    "case": "bash test.sh exits 0",
                    "file": "",
                    "testName": "",
                    "want": "the judging command itself",
                    "got": "no new case: running it is the test, not something a case pins"
                  }
                ],
                "openQuestions": [],
                "questions": [],
                "rulings": [],
                "summary": "Added one section of five cases to test-repo.sh, one per agents/ page, each asserting wc -w on the page is strictly below its pre-compression count (implementer 1095, planner 2203, researcher 1561, reviewer 2909, test-author 1134). Ran bash test-repo.sh: exactly these five cases FAIL today (each equal to its old count), all 252 other cases still pass — 5 of 257 cases fail. No CLAUDE.md exists or was created beside test-repo.sh, per the plan's instruction."
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 1 is yours:\n  Compress the five agent pages — Every page under agents/ says all it said in fewer words, and the suite still guards it.\nWhat it has to satisfy:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden.\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.\n  - Each page's role, audience, file location and English are unchanged.\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the workflow's dispatched rule text; Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages research:agent-pages.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages tests:agent-pages.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages tests:agent-pages.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "implement:agent-pages.0",
              "at": "2026-08-16T20:54:14.800Z",
              "return": {
                "deviations": [],
                "commands": [
                  "bash test.sh — exit 0, all 8 suites: test-repo.sh (257 cases), test-worktree.sh, node --test on backlog.test.mjs, backlog-changed.test.mjs and read-barrier.test.mjs, and the npm test scripts of tools/argus, tools/argus-ui and tools/log-parser. Nothing skipped or excluded.",
                  "bash test-repo.sh — the plan's inner loop, run before any edit (the five word-cap cases red for the right reason: each page at its old count, nothing else red) and after each page; final run 257 of 257 cases ok, exit 0."
                ],
                "blockers": [],
                "questions": [],
                "rulings": [
                  "Kept the reviewer's compression conservative (2823 of 2909 words) rather than trimming inside its probe, break and correction-round paragraphs further, because those sections' blank-line paragraph boundaries and exactly-one-paragraph term counts are load-bearing for awk-scoped suite cases; wrong costs only a further compression pass in a later increment.",
                  "Trimmed multi-sentence reason tails to one sentence where unkeyed (e.g. dropped the reviewer's 'which is the one thing this run cannot buy back' and the implementer's 'so a correction nobody planned is still a correction somebody checks'), because the plan caps reasons at one sentence and keeps prohibitions keyed elsewhere; wrong costs restoring a clause."
                ],
                "summary": "Compressed the five agent pages below their pre-compression word counts — implementer 1095→996, planner 2203→2099, researcher 1561→1463, reviewer 2909→2823, test-author 1134→1072 — keeping every suite-keyed phrase byte-identical and the reviewer's heading and paragraph structure intact; the duplicated already-red rule on the implementer page now stands once, in the commands bullet. bash test.sh exits 0 with all 257 repo cases green."
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages research:agent-pages.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages tests:agent-pages.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `bash test.sh`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages implement:agent-pages.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages implement:agent-pages.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "review:agent-pages.0",
              "at": "2026-08-16T21:08:16.105Z",
              "return": {
                "findings": [
                  {
                    "claim": "The compressed researcher page no longer forbids the researcher writing the codemap itself.",
                    "reproduction": "Criterion 'Everything the old text of these pages required is still required, and everything it forbade is still forbidden', unmet, shown by agents/researcher.md:31: the merge-base sentence read 'say so in your `moduleMap`; you never write the codemap yourself — the planner folds your corrections in on its next call', the compressed sentence keeps only 'say so in your `moduleMap`; the planner folds your corrections in on its next call', and no other sentence on the page carries the prohibition. Fix: reinsert 'you never write the codemap yourself — ' before 'the planner folds' at agents/researcher.md:31; the page stays under its 1561-word cap (1463 + 7 words).",
                    "criterion": "Everything the old text of these pages required is still required, and everything it forbade is still forbidden",
                    "fix": "direct"
                  }
                ],
                "findingCount": 1,
                "allDirect": true,
                "reason": "agents/researcher.md dropped the explicit prohibition 'you never write the codemap yourself' during compression, so something the old text forbade is no longer forbidden anywhere on the page.",
                "head": "27a8a9338527c7c3d8d9df8c8a06dff6ed8aab63",
                "questions": [],
                "rulings": [],
                "summary": "bash test.sh on the unmodified checkout exits 0 (257 repo cases, 8 suites, all green), and every page is shorter by wc -w (implementer 1095→996, planner 2203→2099, researcher 1561→1463, reviewer 2909→2823, test-author 1134→1072). All five named breaks were applied one at a time in a sandbox worktree and each turned bash test.sh exit 1 with the promised case; the test-repo.sh diff is a pure addition of five word-cap cases, so no case was deleted and no keyed wording needed re-keying. By reading each page against its merge-base text I judged the once-per-rule/imperative/no-self-justification criterion met (drops are justifications and duplicates — e.g. the implementer's pre-existing-red rule now stands once, in the commands bullet) and role, audience, file location and English unchanged; zero probes were run; the one finding is the researcher page's dropped codemap-writing prohibition. Beyond the criteria nothing found: no file outside docs/issues quotes the dropped wording.",
                "commands": [
                  {
                    "command": "bash test.sh",
                    "covered": "all 8 suites, 257 repo cases",
                    "exit": 0
                  }
                ]
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nReview round 0 of increment 1. Check the whole diff against the repository's default branch.\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 1 is yours:\n  Compress the five agent pages — Every page under agents/ says all it said in fewer words, and the suite still guards it.\nWhat it has to satisfy:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden.\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.\n  - Each page's role, audience, file location and English are unchanged.\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the workflow's dispatched rule text; Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nThe commands that count for this increment:\n  - `bash test.sh`\nThe break the plan named for each criterion it named one for:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words — append one page's pre-compression body (git show <merge-base>:agents/planner.md) below its current body so its wc -w count reaches its old count (2203 for planner.md) again; that page's word-cap case in test-repo.sh fails and bash test.sh exits 1.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden — delete from agents/implementer.md the sentence beginning 'Never open `issue.md`' in step 1 of '## How you work'; test-repo.sh's case \"the implementer's never-read-issue rule is a priced prohibition quoting its excuse\" fails and bash test.sh exits 1.\n  - Each page's role, audience, file location and English are unchanged — rename agents/reviewer.md to agents/review.md; test-repo.sh's case 'plugin.json declares every page in agents/ and nothing that is not there' fails and bash test.sh exits 1 (the role, audience and English halves are judged by reading, as the test plan says).\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted — in test-repo.sh's agents/implementer.md codemap case (~line 466), change grep -qi 'codemap' to grep -qi 'codemapp', a key no page wording satisfies; that case fails and bash test.sh exits 1 (the no-case-deleted half is judged by reading the test-repo.sh diff, as the test plan says).\n  - bash test.sh exits 0 — delete the breaks/unbreakable bullet from '## What you record' of agents/researcher.md; test-repo.sh's case 'agents/researcher.md gives both breaks and unbreakable their own template' fails and bash test.sh exits 1.\nThe criteria the plan named no break for, with the reason:\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence — no test can catch it: whether two differently worded sentences state one rule, whether a word is common, and whether a passage is self-justification are semantic judgements no grep, count or command computes; the reviewer judges it by reading each compressed page against its merge-base text.\nRead nothing out of docs/issues/2026-08-16-compress-the-rules/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages review:agent-pages.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages review:agent-pages.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `head`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "implement:agent-pages.1",
              "at": "2026-08-16T21:10:58.735Z",
              "return": {
                "deviations": [],
                "commands": [
                  "bash test.sh — all 8 suites, 903 checks, nothing skipped, exit 0"
                ],
                "blockers": [],
                "questions": [],
                "rulings": [],
                "summary": "Reinserted the dropped prohibition 'you never write the codemap yourself' into the researcher page's merge-base sentence, the single direct-fix finding of review round 0; the full test suite passes and the page stays under its word cap."
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nThis is correction loop 1 of 2 for this increment, and it is a direct-fix round: nobody planned it and nobody wrote a test for it, so the findings are your whole brief. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages review:agent-pages.0 --fields findings` — the findings you correct.\nThe commands that count for this increment:\n  - `bash test.sh`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages implement:agent-pages.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages implement:agent-pages.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "review:agent-pages.1",
              "at": "2026-08-16T21:14:05.692Z",
              "return": {
                "findings": [],
                "findingCount": 0,
                "allDirect": true,
                "reason": "",
                "head": "5026f4a973fc7bc4149cd6cdb4485439fb98402b",
                "questions": [],
                "rulings": [],
                "summary": "The compressed researcher page no longer forbids the researcher writing the codemap itself — addressed: agents/researcher.md:31-32 again reads 'you never write the codemap yourself — the planner folds your corrections in on its next call', and the page is at 1469 words, under its 1561-word cap. bash test.sh, all 8 suites, exit 0. The fix diff touches only that sentence (plus run state); no probes run, no breaks applied in this correction round, and nothing in the fix's blast radius broke."
              },
              "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nReview round 1 of increment 1. This is a correction round: the round before filed the findings below, this round was run to fix them, and that fix is what you judge.\nThe fix's diff is `git diff 27a8a9338527c7c3d8d9df8c8a06dff6ed8aab63..HEAD` (two dots): everything committed since that review.\nThe findings that review filed, all of them:\n  1. The compressed researcher page no longer forbids the researcher writing the codemap itself.\n     Reproduction: Criterion 'Everything the old text of these pages required is still required, and everything it forbade is still forbidden', unmet, shown by agents/researcher.md:31: the merge-base sentence read 'say so in your `moduleMap`; you never write the codemap yourself — the planner folds your corrections in on its next call', the compressed sentence keeps only 'say so in your `moduleMap`; the planner folds your corrections in on its next call', and no other sentence on the page carries the prohibition. Fix: reinsert 'you never write the codemap yourself — ' before 'the planner folds' at agents/researcher.md:31; the page stays under its 1561-word cap (1463 + 7 words).\n     Criterion: Everything the old text of these pages required is still required, and everything it forbade is still forbidden\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 1 is yours:\n  Compress the five agent pages — Every page under agents/ says all it said in fewer words, and the suite still guards it.\nWhat it has to satisfy:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden.\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.\n  - Each page's role, audience, file location and English are unchanged.\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the workflow's dispatched rule text; Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nThe commands that count for this increment:\n  - `bash test.sh`\nThe break the plan named for each criterion it named one for:\n  - Each of agents/implementer.md, agents/planner.md, agents/researcher.md, agents/reviewer.md and agents/test-author.md is shorter than it was, measured in words — append one page's pre-compression body (git show <merge-base>:agents/planner.md) below its current body so its wc -w count reaches its old count (2203 for planner.md) again; that page's word-cap case in test-repo.sh fails and bash test.sh exits 1.\n  - Everything the old text of these pages required is still required, and everything it forbade is still forbidden — delete from agents/implementer.md the sentence beginning 'Never open `issue.md`' in step 1 of '## How you work'; test-repo.sh's case \"the implementer's never-read-issue rule is a priced prohibition quoting its excuse\" fails and bash test.sh exits 1.\n  - Each page's role, audience, file location and English are unchanged — rename agents/reviewer.md to agents/review.md; test-repo.sh's case 'plugin.json declares every page in agents/ and nothing that is not there' fails and bash test.sh exits 1 (the role, audience and English halves are judged by reading, as the test plan says).\n  - Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted — in test-repo.sh's agents/implementer.md codemap case (~line 466), change grep -qi 'codemap' to grep -qi 'codemapp', a key no page wording satisfies; that case fails and bash test.sh exits 1 (the no-case-deleted half is judged by reading the test-repo.sh diff, as the test plan says).\n  - bash test.sh exits 0 — delete the breaks/unbreakable bullet from '## What you record' of agents/researcher.md; test-repo.sh's case 'agents/researcher.md gives both breaks and unbreakable their own template' fails and bash test.sh exits 1.\nThe criteria the plan named no break for, with the reason:\n  - Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence — no test can catch it: whether two differently worded sentences state one rule, whether a word is common, and whether a passage is self-justification are semantic judgements no grep, count or command computes; the reviewer judges it by reading each compressed page against its merge-base text.\nRead nothing out of docs/issues/2026-08-16-compress-the-rules/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages review:agent-pages.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages review:agent-pages.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `head`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            }
          ]
        }
      ]
    },
    {
      "id": "workflow-text",
      "title": "Compress the workflow's dispatched rule text",
      "goal": "The rule text workflows/agile-loop.js hands agents is compressed, with the workflow's behavior intact.",
      "criteria": [
        "The prompt and schema-description strings in workflows/agile-loop.js — the rule text agents receive — are shorter than they were, measured in words.",
        "Everything the old strings required is still required, and everything they forbade is still forbidden; each instruction is stated once.",
        "The compressed strings use short imperative sentences in common words; no reason runs past one sentence, and history and self-justification are gone.",
        "Only string content changes: the workflow's code paths and behavior are unchanged, and the file's role, audience, location and English are unchanged.",
        "Every test-repo.sh case keyed on wording this file changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.",
        "bash test.sh exits 0."
      ],
      "depth": "full",
      "status": "todo",
      "note": "Second because it carries this run's own unknown: rule text embedded in code. If research shows these strings are not what the issue means by a workflow page, that comes back as a codemap correction and the increment is re-cut or dropped. agent-pages proved the pattern that keeps suite-keyed wording byte-identical so the re-key criterion holds vacuously; its research step (attempt 1) records it.",
      "branch": "",
      "steps": [
        {
          "label": "research:workflow-text.0",
          "at": "2026-08-16T21:23:59.325Z",
          "return": {
            "needsTests": false,
            "plan": "Compress every dispatched prompt string and every schema `description` string in workflows/agile-loop.js. Keep everything the old strings required and forbade, state each instruction once, use short imperative sentences, and give no reason longer than one sentence.\n\nSCOPE. In scope: (a) the prompt-builder helpers — checkList, breakList, findingList, recordStep, readStep (its `what` argument texts at call sites), readBlock intros, answeredBlock, questionBlock (via answeredBlock), scope, codemapBlock, branchBlock, runOutcome — and (b) every inline prompt handed to agent(): the load-state prompt, decompose, research, tests, implement, review (incrementRange and correctionScope included), replan, publish; and (c) every `description` value in INDEX_STEP, STATE, BACKLOG, PLAN, TESTS, BUILD, VERDICT and PUSH. Out of scope, do not touch: the `meta` object (the harness parses it as a pure literal and it is not text agents receive), every comment, every log() string (they reach the human chat, not agents, and test-repo.sh w20 keys on a log line), all code — identifiers, control flow, conditionals, schema property names, `required` arrays, the payload field lists (PLAN_PAYLOAD etc.), and the exact rendering formats of helper commands. The whole diff is string content; that is acceptance criterion 4 and also what makes the word-count check valid.\n\nDESIGN DECISION. Keep every literal that test-repo.sh asserts on verbatim, and compress the prose around it. test-repo.sh runs the workflow with a stub agent (driver modes w1–w36, lines ~820–2280) and asserts prompt and schema content by literal substring; keeping those literals means zero re-keying, zero test edits, and criterion 5 holds with no case touched. Rejected: rewording keyed phrases and re-keying the ~40 assertion sites — more churn and risk for marginal word savings, since the keyed phrases are already terse and are exactly the binding rules.\n\nINVIOLABLE LITERALS (keep verbatim, per site):\n- recordStep(): the word 'record' and 'backlog.json' in the record line; the words 'verbatim' and 'prompt' describing the prompt file; the literal sequence '<the return file> <the prompt file>'; the field bullets rendered as two-space '- `name`'; and the conditional clause 'push the commit' present exactly when pushes is true and absent otherwise (w1, w8, w36 assert both directions).\n- readStep(): the rendered command '  - `steps <dir>/backlog.json <id> <label> --fields <fields>` — <what>' exactly; the driver rebuilds this string. Only the `what` texts may be shortened. Never emit a 'steps <dir>/backlog.json' line without --fields anywhere except the replan prompt (global driver assertion).\n- codemapBlock(): must contain 'codemap <dir>/backlog.json'.\n- scope(): the phrase 'increment <n> is yours' and the criteria as '  - <criterion>' bullets (w15, w20). Keep 'works', not 'read', beside issue.md — a line pairing issue.md with the word read fails a global implement-prompt assertion.\n- checkList(): empty branch keeps 'No command counts for this increment' (w20); the non-empty header is free; keep the backticked bullet per check.\n- breakList(): keep all three literals and their branch placement: 'No plan named a break for any criterion of this increment.' (empty case), 'The break the plan named for each criterion it named one for:' and 'The criteria the plan named no break for, with the reason:' — w6/w21/w31 assert presence and absence per branch.\n- findingList(): keep interpolation of claim, reproduction and criterion per finding (w29 asserts the interpolated markers).\n- answeredBlock(): no-answer branch keeps 'The human recorded no answer' (w9); never write the text '## Decisions' into it (w19 asserts its absence).\n- branchBlock() create: keep 'branch <dir>/backlog.json <id> <branch>' helper call, a line that pushes the issue branch (and never the increment branch), and 'git checkout -b <branch>` ' with the closing backtick right after the name (w12/w23 assert the backtick). Continue form: keep 'git checkout <branch>'. No 'git fetch'. Never put a push word and the increment branch name on one line (w1, w17 filter line-by-line).\n- load-state prompt: keep 'index <dir>/backlog.json', '## Decisions', '<dir>/issue.md', and the field names rulings and attemptRulings spelled in the prose (w8 regexes). Never write the literal phrase 'read <dir>/backlog.json'; never 'git show origin/', 'merge-base --is-ancestor' or 'git fetch'.\n- tests prompt: must not contain the substring 'review:' or the codemap read (w4).\n- implement prompt: keep readStep fields 'plan,moduleMap,environment' and 'cases' exactly; no '--fields testPlan'; codemap read only on the direct round (w5, w20).\n- review prompt: keep 'Read nothing out of <dir>/backlog.json' (w6/w29 match 'read nothing out of'); keep 'git diff <issueBranch>...HEAD' (three dots) in incrementRange and 'git diff <head>..HEAD' (two dots) in correctionScope; keep the phrase 'correction round' only in correctionScope (w29 asserts presence, w30 absence in the fallback form); no steps read.\n- replan prompt: accepted branch keeps 'Land it first', 'merge', the increment branch name, and a line pushing the issue branch; blocked branch keeps 'do not merge' plus the branch name and no push of it (w17); keep the three reads 'steps <dir>/backlog.json <id>' (whole, no --fields — the one allowed), 'index <dir>/backlog.json', 'codemap <dir>/backlog.json'; recordStep with pushes true.\n- publish prompt: keep 'git push -u origin <issueBranch>'; keep 'Fetch the default branch before you compare'; keep 'Do NOT open <dir>/backlog.json' and never a phrasing where index/steps/codemap/read directly precedes the backlog path; the word 'ruling' may appear only inside the rulings-conditional block, '## Rulings' included (w3/w18 assert a rulings-free prompt when none were recorded); runOutcome keeps the '[<status>]' rendering, 'NOT accepted', and the blocked increment's branch name; no '--delete' anywhere; keep the retry schedule, the attribution footer line, and the do-not-commit/subagent prohibitions as requirements (shorten wording, drop nothing).\n- Schemas: only description strings change; keep 'const VERDICT = {' and its required list intact (w29 regexes the source block); keep the sense 'verbatim, never paraphrased or shortened' in the breaks/unbreakable/attemptBreaks descriptions — it is a requirement of the old text.\n\nWORD BANS for any new wording (static greps over workflows/*.js): no 'handoff' in any spelling; no agent-page file name like researcher.md; no argus, OTEL_, UROBOROS_OBS or collector; no 'git push origin --delete', no 'git push origin :', no prose about deleting a remote branch; no sentence saying nothing an agent or role produced without the word break in it.\n\nWHERE THE FAT IS: the schema descriptions (INDEX_STEP repeats one formula eleven times — one short formula per field suffices), branchBlock's local passage (drop the two quoted rationalisations, keep the rule and one reason), recordStep's framing, the load-state prompt, the replan re-cut paragraph, the publish prompt's step framing and prohibition block, answeredBlock, readBlock intros, readStep what-texts, scope()'s two trailing sentences, correctionScope, runOutcome's header line.",
            "moduleMap": [
              "workflows/agile-loop.js — the plugin's one workflow: meta literal, response schemas (INDEX_STEP, STATE, BACKLOG, PLAN, TESTS, BUILD, FINDING, VERDICT, PUSH), prompt-builder helpers (checkList, breakList, findingList, recordStep, readStep, readBlock, answeredBlock, scope, codemapBlock, branchBlock, runOutcome) and the dispatch loop — entry: the workflow harness runs the file body with args, agent, log, phase; every string handed to agent() and every schema description is the compression target",
              "test-repo.sh — the repository suite: static greps over workflows/*.js plus an embedded node driver (lines ~820–2280) that runs agile-loop.js with a stub agent in modes w1–w36 and asserts prompt and schema content by literal substring — entry: bash test-repo.sh; unchanged under this plan",
              "test.sh — aggregate runner of every suite, the command behind criterion 6 — entry: bash test.sh; unchanged"
            ],
            "environment": [
              "bash test.sh — run from the repository root; runs test-repo.sh, test-worktree.sh, three node --test suites and three npm-based tool suites; needs node and npm on PATH, no install step",
              "bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"' — run from the repository root; 46ae5c6 is the commit this increment started from, present in this checkout, so the baseline does not move as the branch advances",
              "There is no linter or formatter configured in this repository"
            ],
            "testPlan": "WHETHER. No new tests and no test edits. Every executable guard already exists: test-repo.sh's driver cases w1–w36 pin the workflow's dispatch order, schemas and prompt content by literal substring, and its static greps pin the banned words. The plan keeps every asserted literal verbatim, so no case needs re-keying, and what remains of the wording criteria is prose judgment no test can hold. The test-author is not dispatched.\n\nWHAT, per acceptance criterion:\n1. Strings shorter in words — state: the compressed file in the working tree → the wc -w comparison against 46ae5c6 exits 0 — break: restore any compressed string to its old wording (or ship the file unshortened); the comparison exits 1. The whole-file count is a valid measure because criterion 4 confines the diff to string content.\n2. Required and forbidden preserved, each stated once — state: the compressed file → bash test-repo.sh exits 0 — break: delete any one required instruction — the conditional 'push the commit' clause (w1/w8/w36 red), the review prompt's 'Read nothing out of' line (w6 red), 'Land it first' or 'do not merge' in replan (w17 red), the loader's index read or Decisions lift (w1 red), a payload field bullet like '- `breaks`' (w31 red), 'The human recorded no answer' (w9 red), a diff-range command (w6/w29/w30 red), 'Fetch the default branch before you compare' or 'Do NOT open' (w18 red), 'No command counts for this increment' or a break-list heading (w20/w21/w31 red), 'increment N is yours' (w15/w20 red), '<the return file> <the prompt file>' or 'verbatim' (w8 red). Coverage limit, stated as a decision: requirements no existing case asserts — the retry schedule, the attribution footer, the do-not-commit block, most schema-description content — have no failing test; adding cases for them is new test scope this increment does not ask for, so the reviewer judges them by reading the diff.\n3. Short imperative sentences, one-sentence reasons, no history or self-justification — no test; style is not machine-checkable. See unbreakable.\n4. Only string content changes, behavior unchanged — state: the compressed file → bash test.sh exits 0; w1–w36 pin the code paths — break: make recordStep append 'push the commit' unconditionally (w1/w8/w36 red); break the meta literal or the file's syntax (the workflow-parse and meta-purity cases red).\n5. Keyed cases re-keyed, none deleted — under this plan no keyed wording changes, so the criterion is satisfied with zero test edits, and the suite itself enforces the pairing — break: reword a keyed literal such as 'No command counts for this increment' in checkList without re-keying its w20 assertion; bash test-repo.sh goes red. The no-deletion half has no test — deleting a case cannot fail a suite — and is the reviewer's reading of the diff (no test-repo.sh hunks expected at all).\n6. bash test.sh exits 0 — the command is its own case — break: any parse error in workflows/agile-loop.js.\n\nHOW. All cases run at integration level through the existing suite: /home/user/uroboros/test-repo.sh (bash plus an embedded node driver), run alone as `bash test-repo.sh`, and through the aggregate `bash test.sh`. The word-count case is the bash one-liner in the done list. There is no suite doc (CLAUDE.md) covering test-repo.sh; since this round writes no test, that gap triggers no doc work.\n\nWHAT COUNTS AS DONE — the closed list, runnable from the repository root; nobody runs anything else:\n1. bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"'\n2. bash test.sh\n\nWHAT IS ALREADY RED. Nothing was run; the list above is unrun. No decision in this plan turned on a fact only a run could settle — every asserted literal and code path was read from test-repo.sh's driver source and the workflow file.",
            "checks": [
              "bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"'",
              "bash test.sh"
            ],
            "breaks": [
              "The prompt and schema-description strings in workflows/agile-loop.js are shorter than they were, measured in words — restore any compressed string to its old longer wording (or ship the file unshortened): the wc -w comparison against commit 46ae5c6 exits 1",
              "Everything the old strings required is still required, and everything they forbade is still forbidden; each instruction is stated once — delete a required instruction, e.g. the conditional 'push the commit' clause in recordStep or the 'Read nothing out of' line in the review prompt: test-repo.sh driver cases w8 and w6 go red",
              "Only string content changes: the workflow's code paths and behavior are unchanged — change a code path, e.g. make recordStep append 'push the commit' regardless of its pushes flag: driver cases w1, w8 and w36 go red; a syntax or meta-literal change fails the workflow-parse case",
              "Every test-repo.sh case keyed on wording this file changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted — reword a keyed literal, e.g. 'No command counts for this increment' in checkList, without re-keying its w20 assertion: bash test-repo.sh goes red",
              "bash test.sh exits 0 — introduce a parse error into workflows/agile-loop.js: the workflow-parse case fails and bash test.sh exits 1"
            ],
            "unbreakable": [
              "The compressed strings use short imperative sentences in common words; no reason runs past one sentence, and history and self-justification are gone — style is prose judgment; no command or grep distinguishes compliant wording from non-compliant, so the reviewer reads the diff for it"
            ],
            "questions": [],
            "rulings": [
              "Keep every literal test-repo.sh asserts on verbatim and compress the prose around it, because re-keying ~40 assertion sites buys marginal word savings at real risk; wrong costs a somewhat less compressed file",
              "The meta object, log() strings and comments stay untouched, because meta must stay a pure literal and none of the three is rule text dispatched to agents, and criterion 4 confines the diff to string content; wrong costs one follow-up edit",
              "Criterion 1 is measured as whole-file wc -w against commit 46ae5c6, valid because only strings change; wrong costs swapping in a per-string extraction count",
              "needsTests is false: the existing suite already holds every executable guard and this plan changes no test; wrong costs dispatching the test-author in a correction round"
            ],
            "summary": "Planned the compression of agile-loop.js's dispatched prompt and schema-description strings: every literal test-repo.sh keys on stays verbatim so no case needs re-keying, the fat comes out of the schema descriptions and the prose around the fixed phrases, and the work is judged by a word-count comparison against the pre-increment commit plus bash test.sh."
          },
          "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 2 is yours:\n  Compress the workflow's dispatched rule text — The rule text workflows/agile-loop.js hands agents is compressed, with the workflow's behavior intact.\nWhat it has to satisfy:\n  - The prompt and schema-description strings in workflows/agile-loop.js — the rule text agents receive — are shorter than they were, measured in words.\n  - Everything the old strings required is still required, and everything they forbade is still forbidden; each instruction is stated once.\n  - The compressed strings use short imperative sentences in common words; no reason runs past one sentence, and history and self-justification are gone.\n  - Only string content changes: the workflow's code paths and behavior are unchanged, and the file's role, audience, location and English are unchanged.\n  - Every test-repo.sh case keyed on wording this file changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/2026-08-16-compress-the-rules/backlog.json`, before you begin.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text research:workflow-text.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text research:workflow-text.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `breaks`\n  - `unbreakable`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
        },
        {
          "label": "implement:workflow-text.0",
          "at": "2026-08-16T21:33:48.096Z",
          "return": {
            "deviations": [],
            "commands": [
              "bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"' — old=10531, new=10251, exit 0",
              "bash test.sh — all 8 suites, test-repo.sh 257 cases and test-workflows.sh 4 cases included, exit 0, nothing skipped"
            ],
            "blockers": [],
            "questions": [],
            "rulings": [],
            "summary": "Compressed every dispatched prompt string and schema description in the agile-loop workflow from 10531 to 10251 words, keeping every literal the repository suite keys on verbatim; the word-count check and the full suite both pass."
          },
          "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nYour brief is the plan the researcher wrote. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text research:workflow-text.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\nNo test was written for this round — the plan asked for none.\nThe commands that count for this increment:\n  - `bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"'`\n  - `bash test.sh`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text implement:workflow-text.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text implement:workflow-text.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
        },
        {
          "label": "review:workflow-text.0",
          "at": "2026-08-16T21:46:08.486Z",
          "return": {
            "findings": [],
            "findingCount": 0,
            "allDirect": true,
            "reason": "",
            "head": "a32d8b46df84bfaf06150b89a3af4132fb31701d",
            "questions": [],
            "rulings": [
              "Judged the increment as the range 46ae5c6..HEAD, because the rest of the branch-vs-default diff is increment 1's work that earlier review rounds already ruled on; wrong costs a defect in those already-reviewed files going unflagged until the pull request review."
            ],
            "summary": "Both listed commands are green in the unmodified checkout: the wc -w comparison passes (10251 < 10531 words) and bash test.sh exits 0 with all 8 suites and 257 cases passing. The diff changes string-literal content in workflows/agile-loop.js only; every dropped clause was a reason, history or self-justification, every requirement and prohibition survives, and the style criterion the plan named no break for is met on reading. All five named breaks were applied one at a time in a sandbox worktree and each turned a listed command red: the unshortened file fails the wc test (exit 1), deleting the conditional push-the-commit clause fails 3 driver cases, hardcoding it fails 2, rewording the checkList literal without re-keying fails the direct-increment case, and a parse error fails the suite (exit 1). test-repo.sh is untouched because the compression preserved every keyed literal, so no re-keying was needed and no case was deleted; no probes were run; a repo-wide grep found no file outside docs/issues records quoting the old wording, so nothing went stale. Observation: the compression is modest, about 2.7 percent of the file's words."
          },
          "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nReview round 0 of increment 2. Check the whole diff against the repository's default branch.\nThis run works docs/issues/2026-08-16-compress-the-rules/issue.md one increment at a time, and increment 2 is yours:\n  Compress the workflow's dispatched rule text — The rule text workflows/agile-loop.js hands agents is compressed, with the workflow's behavior intact.\nWhat it has to satisfy:\n  - The prompt and schema-description strings in workflows/agile-loop.js — the rule text agents receive — are shorter than they were, measured in words.\n  - Everything the old strings required is still required, and everything they forbade is still forbidden; each instruction is stated once.\n  - The compressed strings use short imperative sentences in common words; no reason runs past one sentence, and history and self-justification are gone.\n  - Only string content changes: the workflow's code paths and behavior are unchanged, and the file's role, audience, location and English are unchanged.\n  - Every test-repo.sh case keyed on wording this file changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.\n  - bash test.sh exits 0.\nDeliberately not yours, and not a gap — a later increment takes each of these: Compress the four skill pages; Compress the rulebook and the pages under .claude/rules/.\nThe commands that count for this increment:\n  - `bash -c 'old=$(git show 46ae5c6:workflows/agile-loop.js | wc -w); new=$(wc -w < workflows/agile-loop.js); test \"$new\" -lt \"$old\"'`\n  - `bash test.sh`\nThe break the plan named for each criterion it named one for:\n  - The prompt and schema-description strings in workflows/agile-loop.js are shorter than they were, measured in words — restore any compressed string to its old longer wording (or ship the file unshortened): the wc -w comparison against commit 46ae5c6 exits 1\n  - Everything the old strings required is still required, and everything they forbade is still forbidden; each instruction is stated once — delete a required instruction, e.g. the conditional 'push the commit' clause in recordStep or the 'Read nothing out of' line in the review prompt: test-repo.sh driver cases w8 and w6 go red\n  - Only string content changes: the workflow's code paths and behavior are unchanged — change a code path, e.g. make recordStep append 'push the commit' regardless of its pushes flag: driver cases w1, w8 and w36 go red; a syntax or meta-literal change fails the workflow-parse case\n  - Every test-repo.sh case keyed on wording this file changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted — reword a keyed literal, e.g. 'No command counts for this increment' in checkList, without re-keying its w20 assertion: bash test-repo.sh goes red\n  - bash test.sh exits 0 — introduce a parse error into workflows/agile-loop.js: the workflow-parse case fails and bash test.sh exits 1\nThe criteria the plan named no break for, with the reason:\n  - The compressed strings use short imperative sentences in common words; no reason runs past one sentence, and history and self-justification are gone — style is prose judgment; no command or grep distinguishes compliant wording from non-compliant, so the reviewer reads the diff for it\nRead nothing out of docs/issues/2026-08-16-compress-the-rules/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text review:workflow-text.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json workflow-text review:workflow-text.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `head`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
        }
      ],
      "attempts": []
    },
    {
      "id": "skill-pages",
      "title": "Compress the four skill pages",
      "goal": "Every SKILL.md under skills/ says all it said in fewer words, and the suite still guards it.",
      "criteria": [
        "Each of skills/agent-brief/SKILL.md, skills/argus/SKILL.md, skills/grill/SKILL.md and skills/retro/SKILL.md is shorter than it was, measured in words.",
        "Everything the old text of these pages required is still required, and everything it forbade is still forbidden.",
        "Each page states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.",
        "Each page's role, audience, file location, frontmatter contract and English are unchanged.",
        "Every test-repo.sh case keyed on wording these pages changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.",
        "bash test.sh exits 0."
      ],
      "depth": "full",
      "status": "todo",
      "note": "agent-pages proved the compress-and-cap pattern; its steps are the precedent. skills/agent-brief/SKILL.md is the densest-keyed page in this group.",
      "branch": "",
      "steps": [],
      "attempts": []
    },
    {
      "id": "session-rules",
      "title": "Compress the rulebook and the pages under .claude/rules/",
      "goal": "rulebook.md (with its GEMINI.md mirror) and both pages under .claude/rules/ say all they said in fewer words.",
      "criteria": [
        "Each of rulebook.md, .claude/rules/agents.md and .claude/rules/authoring.md is shorter than it was, measured in words.",
        "Everything the old text of these documents required is still required, and everything it forbade is still forbidden.",
        "Each document states each rule once, in short imperative sentences of common words; history, anecdotes and self-justification are gone, and no reason runs past one sentence.",
        "Each document's role, audience, file location, frontmatter and English are unchanged.",
        "GEMINI.md is byte-identical to rulebook.md after the change.",
        "Every test-repo.sh case keyed on wording these documents changed is re-keyed to the new wording and still turns red when its rule is removed; no case is deleted.",
        "bash test.sh exits 0."
      ],
      "depth": "full",
      "status": "todo",
      "note": "Last because it is the smallest group and the pattern is proven by then; GEMINI.md is a symlink today, so the mirror holds unless an edit breaks the link.",
      "branch": "",
      "steps": [],
      "attempts": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-16T20:27:22.487Z",
        "return": {
          "questions": [],
          "rulings": [
            "Treat the dispatched prompt and schema-description strings in workflows/agile-loop.js as the issue's workflow page, because workflows/ holds no markdown and those strings are the only workflow rule text agents receive; wrong costs one reverted increment.",
            "Exclude README.md and every CLAUDE.md (skills/, skills/agent-brief/assets/, hooks/, tools/*) from the scope, because the issue's scope list is closed and names none of them; wrong costs a follow-up issue to compress them."
          ],
          "summary": "Cut into four increments, one per rule-document class, each carrying the issue's quality criteria scoped to its own files plus the re-keying of the test-repo.sh cases its wording changes, and each closing with bash test.sh at exit 0 — a single all-documents increment was rejected as one unreviewable 15-file diff, and a separate re-keying increment was rejected because it would leave the suite red between increments. All four run full: every document group is dense with wording-keyed test-repo.sh cases and the compression wording is left to decide. Criterion mapping: compress each agent page → agent-pages; compress each workflow page → workflow-text; compress each skill page → skill-pages; compress rulebook.md and each page under .claude/rules/ → session-rules; keep GEMINI.md byte-identical to rulebook.md → session-rules; the cross-cutting criteria (shorter in words, keep every binding rule, state each rule once, short imperative sentences, drop history with reasons of at most one sentence, role/audience/location unchanged, English, re-key test-repo.sh cases without deleting any, bash test.sh exits 0) are restated inside each increment scoped to its own documents, so each scoped instance lands in exactly one increment; leave docs/issues/ untouched binds every increment and no increment touches it."
        },
        "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nOpen the run state for docs/issues/2026-08-16-compress-the-rules/issue.md: map the issue against the codebase, decide the cut, and write both into docs/issues/2026-08-16-compress-the-rules/backlog.json with the `init` subcommand of the backlog helper your shared brief names, with workflow \"agile-loop\" and the whole codemap in the payload's `codemap` field.\nThis run works at most 8 increments, so a cut that needs more than that is a cut that is too fine.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json - decompose <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json - decompose <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `rulings`\n  - `summary`\n"
      },
      {
        "label": "replan:agent-pages",
        "at": "2026-08-16T21:16:11.638Z",
        "return": {
          "questions": [],
          "rulings": [
            "Kept the three open increments' criteria and order unchanged rather than folding the verbatim-keys strategy into their criteria, because that strategy is the researcher's how, proven and recorded in agent-pages' attempt, and baking it into criteria would turn the cut into a work order; wrong costs a researcher rediscovering the pattern in one research step.",
            "Did not open a further increment to compress agents/reviewer.md deeper (2823 of 2909 words, the shallowest cut), because the criterion was shorter-than-it-was and the review accepted it — its paragraph structure is load-bearing for awk-scoped suite cases, so a deeper cut buys risk the issue's criteria do not ask for; wrong costs a follow-up issue for one page."
          ],
          "summary": "Closed agent-pages as done (review accepted at round 1; worked on the issue branch, nothing to merge) and re-cut nothing structurally: the remaining three increments — workflow-text, skill-pages, session-rules — stand as cut, since the increment surfaced no new files, no ambiguous criteria and no reason to split or merge. The codemap now marks the five agent pages done, records that reviewer.md's paragraph boundaries are load-bearing for awk-scoped cases, and notes on test-repo.sh the proven cheaper route: keep keyed phrases byte-identical and cap shrinkage with wc -w cases. 3 of at most 8 increments remain, workflow-text next."
        },
        "prompt": "Issue directory: docs/issues/2026-08-16-compress-the-rules\nIncrement agent-pages — Compress the five agent pages — has been worked. The review accepted it.\nWhat this increment turned up is what you re-cut against, and it is in the run state. Read it with the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-16-compress-the-rules/backlog.json agent-pages` — every step this increment recorded, whole.\n  - `index docs/issues/2026-08-16-compress-the-rules/backlog.json` — the rest of the cut, with no step content in it.\n  - `codemap docs/issues/2026-08-16-compress-the-rules/backlog.json` — the map as it stands.\nThen close that increment with `close` and re-cut what is still open with `init`, the whole codemap in its `codemap` field. 1 of at most 8 increments are spent.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-16-compress-the-rules/backlog.json - replan:agent-pages <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-16-compress-the-rules/backlog.json - replan:agent-pages <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `rulings`\n  - `summary`\n"
      }
    ],
    "attempts": []
  }
}
