{
  "version": 1,
  "issue": "docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md",
  "workflow": "agile-loop",
  "codemap": "",
  "increments": [
    {
      "id": "i1",
      "title": "A correction round's researcher is sent to its own previous plan",
      "goal": "A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.",
      "criteria": [
        "A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.",
        "The prompt names that step and its field and copies no plan text into itself.",
        "The step the prompt names belongs to the increment being worked and never to another increment.",
        "Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.",
        "Every page that describes what a correction round's researcher reads describes this too.",
        "A test turns red when the prompt no longer names the previous round's plan.",
        "`bash test.sh` exits 0."
      ],
      "depth": "full",
      "status": "done",
      "note": "Review accepted the increment; nothing else open, run complete.",
      "branch": "",
      "steps": [],
      "attempts": [
        {
          "closedAs": "done",
          "at": "2026-08-17T14:25:25.327Z",
          "steps": [
            {
              "label": "research:i1.0",
              "at": "2026-08-17T13:30:15.307Z",
              "return": {
                "needsTests": true,
                "plan": "# What gets built\n\nA correction round's researcher dispatch gains one more read: the research step\nof the round *before*, with `--fields plan`. No plan text is copied into the\nprompt — the read is the channel, exactly as the codemap read already is. Two\nfiles change under the implementer's hand: `workflows/agile-loop.js` (the prompt\nbuilder) and `agents/researcher.md` (the only page that describes what a\ncorrection round's researcher reads). `test-repo.sh` is the test-author's file\nfor this increment; the implementer changes nothing in it.\n\nLine numbers below are as of commit `ac6e705`.\n\n## 1. `workflows/agile-loop.js`\n\n### (a) A per-round label, mirroring `previousTestsLabel`\n\nThe script already tracks \"what the round before left behind\" for the\ntest-author, with a pair of variables: `testsLabel`, set inside the round, and\n`previousTestsLabel`, copied from it at the top of the next round. Build the\nplan label the same way, and nothing new:\n\n- Inside the increment loop, beside `let planLabel = ''`, `let testsLabel = ''`\n  and `let previousTestsLabel = ''` (lines 1104-1106), add\n  `let previousPlanLabel = ''`. It is declared **inside** the increment loop and\n  never at module scope beside `lastChecks` (line 1018): `lastChecks`\n  deliberately crosses increment boundaries because a command judges whatever\n  code is in front of it, while a plan is written against one increment's\n  criteria, and a researcher sent to another increment's plan is sent to a design\n  that is not its own.\n- At the top of the round loop, immediately before `previousTestsLabel =\n  testsLabel` (line 1110), add `previousPlanLabel = planLabel`.\n- In the `if (directFix || directRound)` branch, beside `testsLabel = ''`\n  (line 1135), add `planLabel = ''`. This reset is what makes the round-before\n  rule true in the middle of an attempt: a round that dispatched no researcher\n  must leave no plan label behind, so a round 2 that follows a direct-fix round 1\n  names nothing rather than reaching back over it to round 0. In that branch\n  `planLabel` is read by nobody — the implementer's `directFix` and `directRound`\n  arms of the prompt never touch it — so clearing it costs nothing.\n\n### (b) The read itself\n\nIn the researcher dispatch (lines 1140-1167), inside the\n`round === 0 ? '' : readBlock(...)` expression, add a third entry to the list,\nbetween the `review ... findings` line and the `tests ... openQuestions` line:\n\n```js\npreviousPlanLabel\n  ? readStep(\n      task.id,\n      previousPlanLabel,\n      'plan',\n      `the plan you wrote for the round before: correct that design instead of ` +\n        `deriving it again.`,\n    )\n  : '',\n```\n\nThree things about it are load-bearing:\n\n- The increment id passed to `readStep` is `task.id`, the increment being worked,\n  and the label is `previousPlanLabel`, which only ever holds a label of that same\n  increment because the variable is declared inside the increment loop.\n- The field list is `plan` alone — not `plan,moduleMap`, not the whole step. The\n  issue settles this: the findings and the open questions already reach this\n  researcher by their own reads, and the `moduleMap` is this round's to rewrite.\n- The guard is what criterion 4 rests on. `.filter(Boolean)` at the end of the\n  list (line 1162) already drops an empty string, so a round whose predecessor\n  dispatched no researcher renders a `readBlock` with the remaining lines and\n  stays a valid prompt.\n\nBecause the whole expression sits behind `round === 0 ? '' : ...`, round 0 is\nuntouched and no first-round researcher is sent anywhere.\n\n### (c) The comment above the dispatch\n\nLines 1138-1139 read: \"What the round before left for this one, all of it in the\nstate: the review's findings, and the questions the test-author left open.\"\nRewrite it so it names the third thing and why it is a read rather than a copy:\nthe plan the round before wrote is the design this round corrects, it is the\nlargest thing any agent of this run produces, and the state is where it already\nis. Name the empty branch in the same comment: a round whose predecessor\ndispatched no researcher names no plan step at all.\n\n### What must not change in this file\n\n- No schema and no payload changes. `PLAN` (line 313), `PLAN_PAYLOAD` (line 503),\n  `INDEX_STEP` and `STATE` stay exactly as they are: the plan text never enters\n  this script in the first place — the researcher's structured return carries\n  only `needsTests`, `checks`, `breaks`, `unbreakable`, `questions` and `summary`\n  — which is why criterion 2's \"copies no plan text\" is a property to keep, not\n  one to build.\n- The test-author dispatch (lines 1180-1194) keeps reading `planLabel` — this\n  round's plan — and gains nothing.\n- The implementer dispatch (lines 1200-1256) is untouched; the issue puts the\n  other roles' prompts out of scope.\n\n## 2. `agents/researcher.md`\n\nTwo edits, both in the page's own voice, and no rule stated twice.\n\n### (a) `## Correction rounds` (lines 118-128)\n\nIts first two sentences today name two reads: the review's findings and the\ntest-author's open questions. Add the third as a rule keyed to an observable\npredicate — the prompt either names the step or it does not — with each branch\nstated on its own, per `.claude/rules/authoring.md`'s last row. Write these two\nsentences **verbatim**, as the second paragraph of that section (the test greps\nfor the first of them):\n\n    Where your prompt names the research step of the round before, read its\n    `plan` first: it is the design you already wrote, and this round corrects it\n    instead of deriving it again from the codebase. Where it names no such step,\n    the round before dispatched no researcher and there is nothing to correct.\n\nLeave the section's closing sentences as they stand — \"Nothing carries over from\nthe earlier rounds — the return you record is what binds now, and a case you do\nnot repeat in it is not asked for again.\" That sentence already says the thing\nthe new rule must not contradict and must not repeat: what you read is last\nround's design, but what you record still has to stand on its own, because the\nimplementer of this round reads this round's step and no other.\n\n### (b) The frontmatter `description` (line 3)\n\nIt says the researcher is run \"again for each correction round, where it reads\nthe reviewer's findings out of the run state and turns them into a correction\nplan\". That is a description of what such a researcher reads, so it names this\ntoo: make the clause read \"where it reads the reviewer's findings and the plan\nthe round before wrote out of the run state and turns them into a correction\nplan\". Keep the rest of the description as it is — it names an occasion and\nhands back a result, and must not start walking through steps.\n\nTwo phrases stay off this page whatever the wording: `counts as tested` and\n`dependency footprint`. `test-repo.sh` asserts the shared brief is their only\nowner among agent pages, and using either turns those cases red.\n\n## 3. What is deliberately not touched\n\n- `README.md`. Its researcher row (line 81) says what the role produces and its\n  loop diagram (line 61) says a correction round exists; neither describes what\n  such a researcher reads, so criterion 5 does not reach them.\n- `hooks/read-barrier.mjs`. Its `RULES` table gates the implementer, the reviewer\n  and the test-author; the researcher is deliberately absent, so its read of its\n  own previous `plan` passes without a change there.\n- `skills/agent-brief/SKILL.md`. It says how any agent takes its brief, and never\n  what a correction round's researcher reads.\n- The other roles' prompts, the codemap, and the researcher's return schema — all\n  out of scope by the issue.\n\n## Rejected alternatives\n\n- **Copying the previous plan into the prompt.** It is the largest thing any\n  agent of a run produces (the issue measures 23,700 characters per researcher\n  step), and the run state already carries it. Copying would also break criterion\n  2 and put content back into a script that has been emptied of it on purpose.\n- **Reading the last researcher step rather than the round before's.** A single\n  module-scope \"last plan label\", carried the way `lastChecks` is, is fewer lines\n  — and is what criteria 3 and 4 exist to forbid: it hands an increment worked\n  after a direct one, or a round worked after a direct-fix one, a plan written\n  for something else.\n- **`--fields plan,moduleMap`.** The module map is the field the researcher is\n  told to correct where the codemap is wrong; carrying last round's would invite\n  a copy instead of a fresh reading, and the issue names the plan alone.\n",
                "moduleMap": "/home/user/uroboros/workflows/agile-loop.js — the agile-loop orchestrator: every dispatch schema, the prompt assembly and the increment/round loop; the only file that builds a researcher prompt — entry points: `MAX_CORRECTIONS = 2` (line 61), `PLAN` schema (line 313), `PLAN_PAYLOAD` (line 503), `checkList()` (line 536), `recordStep()` (line 599), `readStep()` (line 624), `readBlock()` (line 634), `isDirectFixRound()` (line 677), `scope()` (line 687), `step()` (line 872), `codemapBlock()` (line 982), `let lastChecks = []` (line 1018), the increment loop `for (let n = worked.length + 1; ; n++)` (line 1053) with its per-increment `let planLabel / testsLabel / previousTestsLabel` (lines 1104-1106), the round loop `for (let round = 0; round <= MAX_CORRECTIONS; round++)` (line 1109) with `previousTestsLabel = testsLabel` (line 1110) and the `directFix || directRound` branch that clears `testsLabel` (lines 1123-1135), and the researcher dispatch with its correction `readBlock` (lines 1137-1168).\n/home/user/uroboros/agents/researcher.md — the researcher’s role page: the role, what it records, the test plan it decides, its correction-round reads and its boundaries — entry points: the frontmatter `description:` (line 3), `## What you record` (lines 40-68), `## The test plan` (lines 70-116), `## Correction rounds` (lines 118-128), `## Boundaries` (lines 130-141).\n/home/user/uroboros/test-repo.sh — the repository’s own rule suite, plain bash, ~3300 lines, sections opened by `echo \"=== <title>\"` and cases counted by the `ok`/`no` helpers (lines 11-12) — entry points: the researcher-page case block `=== the mutation standard has one owner` (lines 472-585), the embedded node driver heredoc `driver.js` (lines 809-2225) with `DISJOINT_MARKERS` (line 832), `readStep()` (line 864), `planReturn` (line 870), `verdictReturnWithFinding` (line 894), `verdictReturnWithDirectFinding` (line 908), `decomposeReturnMixed` (line 921), `contextFor()` (line 1160), the standing per-call assertions at the top of `main()` (lines 1325-1363), the per-mode assertion chain (w10 at line 1659, w16 at 1762, w22 at 1914, w24 at 1936, w36 last), and the `run_driver` registration loop (lines 2237-2273).\n/home/user/uroboros/hooks/read-barrier.mjs — read only, changed by nothing here: its `RULES` table (line 55) gates the implementer, the reviewer and the test-author, and the researcher is deliberately absent, so a researcher reading `--fields plan` of its own previous step passes.\n/home/user/uroboros/README.md — read only: the researcher row (line 81) says what the role produces and the loop diagram (lines 56-61) says a correction round exists; neither describes what such a researcher reads, so criterion 5 does not reach it.",
                "environment": "Repository root is /home/user/uroboros; every command below runs from there.\n- `bash test.sh` — the closed check: it runs `test-repo.sh`, `test-worktree.sh`, `node --test skills/agent-brief/assets/backlog.test.mjs`, `node --test hooks/backlog-changed.test.mjs`, `node --test hooks/read-barrier.test.mjs` and three `npm --prefix tools/<tool> test` suites, and exits 0 only when all pass. Needs `node`, `npm` and `git` on PATH. There is no install step: every suite in it is zero-dependency.\n- `bash test-repo.sh` — the repository suite alone, the one file every new case of this increment lands in, and the command the test plan names for running just that file. Needs `node` (the driver cases spawn node against the workflow source) and `git` (one case runs `git ls-files`). A strict subset of `bash test.sh`.\nThere is no linter and no formatter in this repository, and nothing to install.",
                "testPlan": "# Whether\n\nTests. Every criterion but the last is a fact about a string the workflow builds\nor a sentence a page carries, and both are checkable.\n\nEvery case below lands in `/home/user/uroboros/test-repo.sh` and nowhere else.\nDo not touch `workflows/agile-loop.js` or `agents/researcher.md`: the cases are\nwritten against the behaviour the plan describes, and the implementer builds it.\n\n# What — the cases, by criterion\n\n## Criterion 1 — the prompt names the round-before's research step and orders the read\n\n- **1.1** — mode `w10` (exists; one full increment, `review:i1.0` returns\n  `verdictReturnWithFinding`, so exactly one correction round runs) — the\n  `research:i1.1` dispatch prompt → contains `readStep('i1', 'research:i1.0',\n  'plan')`, i.e. the exact string\n  `steps docs/issues/x/backlog.json i1 research:i1.0 --fields plan` — break:\n  delete the `previousPlanLabel ? readStep(...) : ''` entry from the correction\n  `readBlock` list in the researcher dispatch of `workflows/agile-loop.js`.\n\n## Criterion 2 — it names the step and its field, and copies no plan text\n\n- **2.1** — mode `w10` — the same asserted string as 1.1 carries `--fields plan`,\n  so the field is pinned by it; the driver's standing per-call assertion that no\n  prompt reads a step without `--fields` (already at the top of `main`) covers\n  every other mode — break: change the new read to name no fields\n  (`readStep(task.id, previousPlanLabel, '', ...)`), which the standing\n  assertion and 2.1 both catch.\n- **2.2** — every mode, as a standing per-call assertion — the shared\n  `planReturn` fixture gains a `plan: 'MARKER-PLAN-TEXT'` property (the two\n  fixtures derived from it with `Object.assign` inherit it), `MARKER-PLAN-TEXT`\n  joins `DISJOINT_MARKERS`, and no prompt of any dispatch in any mode contains\n  `MARKER-PLAN-TEXT` → no prompt carries plan text — break: interpolate the\n  researcher's returned plan into the correction prompt (append the round-before\n  return's `plan` to the `readBlock` intro) instead of naming the read.\n\n## Criterion 3 — the step named belongs to the increment being worked\n\n- **3.1** (positive) — mode `w10` — the assertion of 1.1 names `i1` and\n  `research:i1.0` while increment i1 is being worked — break: as 1.1.\n- **3.2** (negative) — new mode `w37`, fixture `{ stateReturn: noState,\n  decomposeReturn: decomposeReturnMixed, researchReturn: planReturn, verdictFor:\n  (label) => (label === 'review:i2.0' ? verdictReturnWithFinding :\n  verdictReturnClean) }` — i1 is full and clean, i2 is the planner's `direct`\n  increment whose round-0 review files one non-direct finding, so i2's round 1\n  is a full correction round whose own previous round dispatched no researcher.\n  Expected labels: `load-state`, `decompose`, `research:i1.0`, `tests:i1.0`,\n  `implement:i1.0`, `review:i1.0`, `replan:i1`, `implement:i2.0`, `review:i2.0`,\n  `research:i2.1`, `tests:i2.1`, `implement:i2.1`, `review:i2.1`, `replan:i2`,\n  `publish`. Assert the `research:i2.1` prompt contains no occurrence of\n  `research:i1` at all → the correction round is never sent to another\n  increment's plan — break: carry the last researcher's label at module scope\n  beside `lastChecks` (one `lastPlanLabel`, set wherever a researcher runs, used\n  in place of the per-increment `previousPlanLabel`), which lets i1's label\n  survive into i2 and renders\n  `steps docs/issues/x/backlog.json i2 research:i1.0 --fields plan`.\n\n## Criterion 4 — no researcher in the previous round: no such step, still valid\n\n- **4.1** — mode `w37` (same fixture and label assertion as 3.2) — the\n  `research:i2.1` prompt matches no `steps docs/issues/x/backlog.json i2\n  research:` read at all, and still contains `readStep('i2', 'review:i2.0',\n  'findings')`, and the label sequence above is unchanged → the prompt names no\n  plan step and stays valid — break: drop the `previousPlanLabel ? ... : ''`\n  guard and render the read unconditionally, which emits a read whose label is\n  empty.\n- **4.2** — new mode `w38`, fixture `{ stateReturn: noState, decomposeReturn:\n  decomposeReturnOne, researchReturn: planReturn, verdictFor: (label) => (label\n  === 'review:i1.0' ? verdictReturnWithDirectFinding : label === 'review:i1.1' ?\n  verdictReturnWithFinding : verdictReturnClean) }` — one full increment whose\n  round-0 review files an all-direct finding (round 1 is a direct-fix round with\n  no researcher) and whose round-1 review files an ordinary one (round 2 is a\n  full round). Expected labels: `load-state`, `decompose`, `research:i1.0`,\n  `tests:i1.0`, `implement:i1.0`, `review:i1.0`, `implement:i1.1`, `review:i1.1`,\n  `research:i1.2`, `tests:i1.2`, `implement:i1.2`, `review:i1.2`, `replan:i1`,\n  `publish`. Assert the `research:i1.2` prompt matches no\n  `steps docs/issues/x/backlog.json i1 research:` read — in particular not\n  `research:i1.0`, the plan two rounds back — and still contains\n  `readStep('i1', 'review:i1.1', 'findings')` — break: delete `planLabel = ''`\n  from the `if (directFix || directRound)` branch of the round loop, so round 0's\n  label survives the direct-fix round and round 2 is sent back to it.\n\n## Criterion 5 — every page that describes these reads describes this one\n\nTwo `ok`/`no` cases under one new `echo \"=== ...\"` heading, placed beside the\nexisting researcher-page cases (the block that starts at\n`=== the mutation standard has one owner`, lines 473-585), each with a comment\nnaming its break, in that block's style.\n\n- **5.1** — `agents/researcher.md`, collapsed to one line the way the existing\n  `break_researcher_collapsed` case does it (`tr '\\n' ' ' <page | tr -s ' '`) →\n  contains, verbatim, `Where your prompt names the research step of the round\n  before, read its \\`plan\\` first` — break: delete that sentence from the page's\n  `## Correction rounds` section.\n- **5.2** — the `description:` line of the same page's frontmatter → contains\n  `the plan the round before wrote` — break: revert the description's correction-\n  round clause to naming the reviewer's findings alone.\n\n## Criterion 6 — a test turns red when the prompt stops naming the plan\n\n- **6.1** — case 1.1 is that test, and its break is the deletion of the read from\n  the dispatch. No second case: a criterion satisfied by the existence of another\n  criterion's case is satisfied by that case running.\n\n## Criterion 7 — `bash test.sh` exits 0\n\nNo case of its own: the command in **What counts as done** is the executable\ncheck, and it turns red when any case above, or any other suite, fails.\n\n# How\n\n- **Level** — unit, all of them. The driver loads `workflows/agile-loop.js`\n  in-process (`new AsyncFunction`) with a stubbed `agent` and asserts on the\n  prompts it built; the page cases are greps. Nothing spawns a run.\n- **File** — `/home/user/uroboros/test-repo.sh`, the only test file this\n  increment touches.\n- **Framework** — plain bash with the file's own `ok`/`no` counters, plus the\n  embedded Node driver in the `cat >\"$driver_tmp/driver.js\" <<'JS'` heredoc\n  (lines 809-2225), which has its own `assertTrue` / `assertEqualArrays` and uses\n  no test framework.\n- **Command that runs just that file** — `bash test-repo.sh`, from\n  `/home/user/uroboros`.\n\n## The suite's conventions, because there is no doc for them\n\nThere is no `CLAUDE.md` governing `test-repo.sh`: the repository root has none,\nand `hooks/CLAUDE.md` documents only the two `node --test` suites under\n`hooks/`. Do not create one — a `CLAUDE.md` at the root would load into every\nsession in this checkout and be inherited by every subagent, which\n`.claude/rules/agents.md` exists to prevent, and every rule page in this\nrepository is path-scoped for exactly that reason. What the suite does instead is\nin its own comments; these are the conventions the new cases follow:\n\n- A driver case is three edits: a `case 'wN':` returning its fixture in\n  `contextFor` (line 1160), a `} else if (mode === 'wN') {` branch in the\n  assertion chain, and a `run_driver \"$wf\" wN \"$wf_name: <what it proves>\"` line\n  in the `for wf in ...` registration loop (line 2237). `w36` is the highest\n  number in use, so the new modes are `w37` and `w38`.\n- Each mode branch opens with a comment saying what case it is and why it exists;\n  each assertion message states what went wrong, never what was expected.\n- Build every read string with the driver's `readStep(id, label, fields)` helper\n  (line 864) or with the `'steps ' + STATE_PATH + ...` form it is made of — never\n  hand-write the path.\n- A new marker string joins `DISJOINT_MARKERS` (line 832); the driver's first\n  assertion proves no marker contains another, so it must not be a substring of,\n  or contain, an existing one.\n- Fixtures are shared module-level constants (`planReturn`, `decomposeReturnMixed`,\n  `verdictReturnWithDirectFinding`, `verdictReturnWithFinding`,\n  `verdictReturnClean`), reused rather than copied; a mode that needs a variant\n  builds it with `Object.assign({}, base, {...})`.\n- Page cases sit under an `echo` heading of their own, use `ok`/`no`, and carry a\n  comment naming the production change that turns them red.\n\n## Faked and real\n\nThe driver fakes exactly one thing — the `agent` function — and runs the real\nworkflow source against it. Nothing else is stubbed; the page cases read the real\nfiles.\n\n# What counts as done\n\n    bash test.sh\n\nOne command, run from `/home/user/uroboros`. Nothing else runs: the two new\ndriver modes, the two amended ones and the two page cases are all inside\n`test-repo.sh`, which `test.sh` runs first, and criterion 7 asks for the whole\nsuite anyway.\n\n# What is already red\n\nNothing was run, and the list is unrun. No decision in this plan turns on a fact\nonly a run can settle: the prompt shape, the round-loop variables, the fixture\nharness and the page's wording were all settled by reading `workflows/agile-loop.js`,\n`test-repo.sh` and `agents/researcher.md`. The first run belongs to whoever runs\nit downstream.\n",
                "checks": [
                  "bash test.sh"
                ],
                "breaks": [
                  "A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix. — delete the `previousPlanLabel ? readStep(...) : ''` entry from the correction `readBlock` list in the researcher dispatch of workflows/agile-loop.js",
                  "The prompt names that step and its field and copies no plan text into itself. — pass an empty fields argument to that read, so it names the whole step; or interpolate the round-before return’s plan text into the prompt instead of naming the read",
                  "The step the prompt names belongs to the increment being worked and never to another increment. — carry the last researcher’s label at module scope beside `lastChecks` (one `lastPlanLabel` set wherever a researcher runs) instead of the per-increment `previousPlanLabel`, which lets increment i1’s label reach an i2 correction round",
                  "Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid. — delete `planLabel = ''` from the `if (directFix || directRound)` branch of the round loop, so a round after a direct-fix round is sent back to the plan two rounds behind it; or drop the `previousPlanLabel` guard so the read renders with an empty label",
                  "Every page that describes what a correction round’s researcher reads describes this too. — delete the new sentence from `## Correction rounds` in agents/researcher.md, or revert that page’s frontmatter description to naming the reviewer’s findings alone",
                  "A test turns red when the prompt no longer names the previous round’s plan. — the same deletion as the first entry: with the read gone, the w10 case that asserts `steps docs/issues/x/backlog.json i1 research:i1.0 --fields plan` fails",
                  "`bash test.sh` exits 0. — any production change that turns a case in any suite red; the command in the checks list is the executable case"
                ],
                "unbreakable": [],
                "questions": [],
                "rulings": [
                  "Read strictly the round before, not the last round that had a researcher, because criterion 4 says a prompt whose previous round dispatched no researcher names no such step; wrong costs a correction round after a direct-fix round re-deriving a design that was two rounds back in the state",
                  "Name only the `plan` field on the new read, because the findings and the open questions already reach this researcher by their own reads and the module map is this round’s to rewrite; wrong costs one more field name in one prompt line",
                  "Write no suite doc for `test-repo.sh` and put its conventions in the test plan instead, because a `CLAUDE.md` at the repository root would load into every session here and be inherited by every subagent, which `.claude/rules/agents.md` exists to prevent; wrong costs the next test-author re-deriving those conventions from the file, which is what happens today",
                  "Update the researcher page’s frontmatter description as well as its body, because that description also states what a correction round’s researcher reads and criterion 5 covers every such description; wrong costs one reverted clause"
                ],
                "summary": "A correction round's researcher will be sent to its own previous round's plan through the run state — one guarded `--fields plan` read in the workflow's correction block, a per-increment `previousPlanLabel` that resets whenever a round dispatches no researcher, and the same rule on the researcher's page — with two new driver modes and two page cases in test-repo.sh proving it, including the direct-increment and direct-fix rounds where no such step exists."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json`, before you begin.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `breaks`\n  - `unbreakable`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "tests:i1.0",
              "at": "2026-08-17T13:35:01.640Z",
              "return": {
                "cases": [
                  {
                    "case": "1.1 — mode w10, the research:i1.1 dispatch prompt contains readStep('i1','research:i1.0','plan')",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "run_driver w10: \"agile-loop.js: a correction round carries the reviewer's findings to the researcher and the reason to the human\" — added assertion \"the correction round's researcher is not sent to read the round-before's plan out of the run state\"",
                    "want": "round1's prompt (research:i1.1) contains the literal string readStep('i1', 'research:i1.0', 'plan'), i.e. 'steps docs/issues/x/backlog.json i1 research:i1.0 --fields plan'",
                    "got": "FAIL — the correction round's researcher is not sent to read the round-before's plan out of the run state (bash test-repo.sh, w10 driver run)"
                  },
                  {
                    "case": "2.1 — the same asserted string as 1.1 carries --fields plan, pinning the field; the standing per-call assertion (no prompt reads a step without --fields) covers every other mode",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "same assertion as 1.1 (w10), plus the pre-existing standing per-call loop 'reads a step without naming the fields it may see'",
                    "want": "the new read names --fields plan explicitly; no prompt in any mode reads a step with no --fields at all",
                    "got": "FAIL — same failure as 1.1 pins this; the standing --fields loop currently passes in every mode (no case broke it)"
                  },
                  {
                    "case": "2.2 — every mode, standing per-call assertion: no prompt of any dispatch contains the researcher's returned plan text (MARKER-PLAN-TEXT)",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "standing loop in main(): '<label> prompt carries the previous round's plan text instead of naming the read that fetches it' — planReturn gained plan: 'MARKER-PLAN-TEXT', joined to DISJOINT_MARKERS",
                    "want": "no call.prompt in any of the 38 driver modes contains MARKER-PLAN-TEXT",
                    "got": "currently passes in every mode (bash test-repo.sh, all w1-w38 runs ok) since no prompt interpolates the plan today; it turns red if a correction dispatch appends the round-before return's plan text into the prompt instead of naming the read"
                  },
                  {
                    "case": "3.1 (positive) — mode w10, the assertion of 1.1 names i1 and research:i1.0 while increment i1 is being worked",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "same assertion as 1.1 (w10)",
                    "want": "the named step is 'i1' / 'research:i1.0' while i1 is the increment worked",
                    "got": "FAIL — same failure as 1.1"
                  },
                  {
                    "case": "3.2 (negative) — new mode w37: i1 full/clean, i2 direct with a round-0 finding, so i2's round 1 researcher must never name increment i1's research step",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "run_driver w37: \"agile-loop.js: a correction round of one increment never names another increment's research step, even when its own round before dispatched no researcher\"",
                    "want": "labels match the given full sequence, and research:i2.1's prompt contains no occurrence of 'research:i1' at all",
                    "got": "currently ok (bash test-repo.sh, w37 driver run) — the fixture's label sequence and the absence of 'research:i1' both hold today, since no cross-increment leak exists yet; it turns red if a fix carries the last researcher's label across increment boundaries instead of scoping it to the increment being worked"
                  },
                  {
                    "case": "4.1 — mode w37: the research:i2.1 prompt matches no 'i2 research:' read at all, and still contains readStep('i2','review:i2.0','findings')",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "run_driver w37 (same test as 3.2), second assertion 'i2's correction-round researcher prompt names a research step of i2, though i2's own round 0 dispatched no researcher for it to read'",
                    "want": "no `steps docs/issues/x/backlog.json i2 research:` read appears in research:i2.1's prompt; the label sequence is unchanged",
                    "got": "currently ok — passes vacuously since the read does not exist yet at all; turns red if an unconditional render emits the read with an empty label"
                  },
                  {
                    "case": "4.2 — new mode w38: a direct-fix round (no researcher) followed by a full round; round 2's researcher must name no plan step at all, not even the plan two rounds back",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "run_driver w38: \"agile-loop.js: a round whose round before was a direct-fix round is sent to no plan step at all, not even the plan two rounds back\"",
                    "want": "labels match the given sequence; research:i1.2's prompt matches no 'i1 research:' read at all (in particular not research:i1.0); still contains readStep('i1','review:i1.1','findings')",
                    "got": "currently ok — passes vacuously since the plan-read feature does not exist yet; turns red if round 0's label is made to survive the direct-fix round instead of being cleared, sending round 2 back to research:i1.0"
                  },
                  {
                    "case": "5.1 — agents/researcher.md, collapsed to one line, contains verbatim 'Where your prompt names the research step of the round before, read its `plan` first'",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "=== a correction round's researcher reads the plan it already wrote — \"agents/researcher.md tells a correction round's researcher to read the round-before's plan first\"",
                    "want": "the collapsed page text contains that sentence",
                    "got": "FAIL — agents/researcher.md does not tell a correction round's researcher to read the round-before's plan (bash test-repo.sh)"
                  },
                  {
                    "case": "5.2 — the description: line of agents/researcher.md's frontmatter contains 'the plan the round before wrote'",
                    "file": "/home/user/uroboros/test-repo.sh",
                    "testName": "=== a correction round's researcher reads the plan it already wrote — \"agents/researcher.md's frontmatter description names the plan the round before wrote\"",
                    "want": "the description: line contains that phrase",
                    "got": "FAIL — agents/researcher.md's frontmatter description does not name the plan the round before wrote (bash test-repo.sh)"
                  },
                  {
                    "case": "6.1 — case 1.1 is the go-red case for this criterion; no second case, since a criterion satisfied by another criterion's case is satisfied by that case running",
                    "file": "",
                    "testName": "",
                    "want": "",
                    "got": "not a separate case — satisfied by case 1.1's test running and failing red, as the plan itself states"
                  },
                  {
                    "case": "7 — bash test.sh exits 0 — no case of its own; the command is the executable check, checked by the implementer once the change exists",
                    "file": "",
                    "testName": "",
                    "got": "not a separate case — the plan names no test for this criterion, only the command itself"
                  }
                ],
                "openQuestions": [],
                "questions": [],
                "rulings": [],
                "summary": "Added driver modes w37 and w38 and one new assertion in the existing w10 case to test-repo.sh, plus two page cases under a new '=== a correction round's researcher reads the plan it already wrote' heading, per the researcher's test plan for increment 1. bash test-repo.sh: 253 ok, 3 FAIL (the w10 plan-read assertion and the two page cases) — all three fail on the missing behaviour itself, confirmed by reading their failure messages; the two new fixture modes w37 and w38 pass today because the cross-increment and cross-round leaks they guard against do not exist yet, and will turn red only if the implementer introduces them."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "implement:i1.0",
              "at": "2026-08-17T13:39:16.216Z",
              "return": {
                "deviations": [],
                "commands": [
                  "bash test.sh — exit 0; all 8 suites pass (test-repo.sh 256 cases, test-worktree.sh, backlog.test.mjs, backlog-changed.test.mjs, read-barrier.test.mjs, three npm tool suites, 23 node --test cases in the last). Nothing skipped or excluded.",
                  "bash test-repo.sh — exit 0 after the change; 256 cases. Before the change it was exit 1 with 3 red, the three the test plan predicted: w10's 'the correction round's researcher is not sent to read the round-before's plan out of the run state', 'agents/researcher.md does not tell a correction round's researcher to read the round-before's plan' and 'agents/researcher.md's frontmatter description does not name the plan the round before wrote'. Run as the subset of test.sh to confirm the planned tests failed for the right reason first."
                ],
                "blockers": [],
                "questions": [],
                "rulings": [
                  "Placed the two verbatim researcher-page sentences as their own paragraph between the test-plan sentence and the 'Nothing carries over from the earlier rounds' closing, because the plan calls them the section's second paragraph and also says the closing sentences stand as they are, which only both hold with the new paragraph before that closing; wrong costs a one-paragraph move in agents/researcher.md."
                ],
                "summary": "A correction round's researcher is now sent to read the plan the round before wrote, as a run-state read rather than copied text, scoped to the increment being worked and cleared by any round that dispatched no researcher; agents/researcher.md states the rule and its empty branch. bash test.sh exits 0."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `bash test.sh`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 implement:i1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 implement:i1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "review:i1.0",
              "at": "2026-08-17T13:59:54.591Z",
              "return": {
                "findings": [
                  {
                    "claim": "The plan's second named break for criterion 4 — dropping the `previousPlanLabel` guard so the plan read renders with an empty step label — leaves `bash test.sh` green, so nothing verifies the 'stays valid' half of that criterion: a correction round after a round that dispatched no researcher can be sent an unrunnable read and no case catches it.",
                    "reproduction": "Doubt stated before probing: does any listed command notice a plan read that names no step? In a sandbox worktree at cc83b36, replace the `previousPlanLabel ? readStep(...) : ''` entry at workflows/agile-loop.js:1162-1170 with an unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)` (the plan's own second break variant for this criterion). `bash test.sh` exits 0 — 256 cases, 8 suites, all pass. Probe: the test-repo.sh driver was copied out of the sandbox and run in mode w38 with a dump of the `research:i1.2` prompt; it printed the line \"  - `steps docs/issues/x/backlog.json i1  --fields plan` — the plan you wrote for the round before: correct that design instead of deriving it again.\" — a read naming an empty step, which the researcher would run and fail on. The w37 and w38 assertions only reject a prompt matching /steps docs\\/issues\\/x\\/backlog\\.json i(1|2) research:/, so an empty label slips through both. The other five named breaks (b1 delete the read entry, empty fields argument, module-scope lastPlanLabel, delete `planLabel = ''` from the direct branch, delete the researcher.md sentence, revert the frontmatter description) each turned test.sh red.",
                    "criterion": "Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.",
                    "fix": "needs-plan"
                  }
                ],
                "findingCount": 1,
                "allDirect": false,
                "reason": "Nothing verifies that a correction round whose previous round dispatched no researcher is sent a valid prompt: removing the guard that suppresses the plan read makes the prompt order a read with no step label at all, and the whole suite stays green, so the criterion that the prompt 'names no such step and stays valid' rests on the production code alone.",
                "head": "cc83b365c774959d19f915d70e33283ffccfe65d",
                "questions": [],
                "rulings": [
                  "Judged the criterion-4 break verified only where every variant the plan named turns a listed command red, because the plan named the empty-label variant as a break for that criterion and it is a mutation of the criterion's own behaviour; wrong costs one correction round adding an assertion nobody needed."
                ],
                "summary": "`bash test.sh` in the unmodified checkout at cc83b36: 256 repository cases plus 8 suites, exit 0, nothing skipped. Applied all six named breaks plus the second variant the plan named for criterion 4 in a sandbox worktree, one at a time: deleting the read entry, emptying its fields argument, carrying the label at module scope, deleting `planLabel = ''` from the direct branch, deleting the researcher.md sentence and reverting its frontmatter description each turned `bash test.sh` red (1-2 cases each, in w10/w37/w38 and the two researcher.md checks); only dropping the `previousPlanLabel` guard stayed green, which is the one finding. Criterion 5 was judged by reading agents/researcher.md, whose `## Correction rounds` section now tells the researcher to read the round-before's `plan` first and whose frontmatter names it too — README.md and rulebook.md describe the loop but not what that researcher reads, so nothing else was stale. Three prompt dumps in the sandbox (one probe, two baselines) confirmed the shipped behaviour: the w10 round-1 prompt names `steps docs/issues/x/backlog.json i1 research:i1.0 --fields plan`, i2's correction round in w37 and the post-direct-fix round 2 in w38 name no plan read at all, and no prompt in any mode carries the plan text itself. Observation, no action needed: the section's opening sentence still says the prompt names 'those two steps' while three reads can now be named."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nReview round 0 of increment 1. Check the whole diff against the repository's default branch.\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe commands that count for this increment:\n  - `bash test.sh`\nThe break the plan named for each criterion it named one for:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix. — delete the `previousPlanLabel ? readStep(...) : ''` entry from the correction `readBlock` list in the researcher dispatch of workflows/agile-loop.js\n  - The prompt names that step and its field and copies no plan text into itself. — pass an empty fields argument to that read, so it names the whole step; or interpolate the round-before return's plan text into the prompt instead of naming the read\n  - The step the prompt names belongs to the increment being worked and never to another increment. — carry the last researcher's label at module scope beside `lastChecks` (one `lastPlanLabel` set wherever a researcher runs) instead of the per-increment `previousPlanLabel`, which lets increment i1's label reach an i2 correction round\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid. — delete `planLabel = ''` from the `if (directFix || directRound)` branch of the round loop, so a round after a direct-fix round is sent back to the plan two rounds behind it; or drop the `previousPlanLabel` guard so the read renders with an empty label\n  - Every page that describes what a correction round's researcher reads describes this too. — delete the new sentence from `## Correction rounds` in agents/researcher.md, or revert that page's frontmatter description to naming the reviewer's findings alone\n  - A test turns red when the prompt no longer names the previous round's plan. — the same deletion as the first entry: with the read gone, the w10 case that asserts `steps docs/issues/x/backlog.json i1 research:i1.0 --fields plan` fails\n  - `bash test.sh` exits 0. — any production change that turns a case in any suite red; the command in the checks list is the executable case\nRead nothing out of docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 review:i1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 review:i1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `head`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "research:i1.1",
              "at": "2026-08-17T14:07:28.614Z",
              "return": {
                "needsTests": true,
                "plan": "WHAT THIS ROUND FIXES\n\nThe review filed one finding, and it is a coverage gap, not a production defect. The workflow already renders the right prompts: `workflows/agile-loop.js` guards the previous-round plan read with `previousPlanLabel ? readStep(task.id, previousPlanLabel, 'plan', ...) : ''` (lines 1162-1170) and sets `planLabel = ''` in the direct/direct-fix branch, so a correction round whose own previous round dispatched no researcher is sent to no plan step. The reviewer probed the run and confirmed that. What no case catches is the removal of that guard: with the read made unconditional the line renders as `  - `steps docs/issues/x/backlog.json i2  --fields plan` — the plan you wrote for the round before: ...`, a read that names an empty step label and that the researcher would run and fail on, and `bash test.sh` still exits 0. The two assertions that should have caught it (test-repo.sh:2288 for w37, test-repo.sh:2300 for w38) match on `/steps docs\\/issues\\/x\\/backlog\\.json i(1|2) research:/`, and an empty label has no `research:` in it, so the malformed read slips through both.\n\nSo this round changes tests only, in `test-repo.sh`, and changes no production code.\n\nTHE CHANGE\n\n1. A cross-mode validity assertion in the driver's `main()`, in the block of checks that run in every mode (test-repo.sh:1373-1422, beside the existing 'reads a step without naming the fields it may see' loop at 1379-1385). For every dispatch whose label does not start with `replan:`, every prompt line that contains `steps ` + `STATE_PATH` names a non-empty step label: after the state path come the increment id and then a step label of non-space characters. A line where the increment id is followed by a second space, or by the closing backtick, names no step and fails, with the call's label and the offending line in the message. The `replan:` exception is load-bearing and is the same exception the existing fields loop makes: the closing planner reads its increment whole with no step label at all (`agile-loop.js:1402` renders `steps <path> <id>` followed directly by the backtick), and that read is correct. Those two sites — `readStep` at agile-loop.js:624 and the closing planner's line at 1402 — are the only places in the workflow that render a `steps` read, so no other shape can false-positive here.\n\n2. w37's second assertion (test-repo.sh:2288) becomes: `research:i2.1`'s prompt carries no plan read at all — no `--fields plan` anywhere in it. The plan read is the only research-step read a researcher prompt ever carries, so this is strictly broader than the `i2 research:` regex it replaces and it catches the empty-label render as well as a wrongly-named one.\n\n3. w38's first assertion (test-repo.sh:2300) becomes the same check for `research:i1.2`'s prompt: no `--fields plan`. That keeps catching the plan two rounds back (which is what deleting `planLabel = ''` from the direct-fix branch would produce) and now also catches the empty-label render.\n\nWHAT STAYS UNTOUCHED\n\nNo production file changes this round. `workflows/agile-loop.js` is correct as committed at cc83b36 and is only the mutation target the new cases are measured against. `agents/researcher.md` already carries criterion 5's sentence ('## Correction rounds': 'Where your prompt names the research step of the round before, read its `plan` first: ...') and the frontmatter clause ('the plan the round before wrote'), both pinned by shell checks at test-repo.sh:594-609. Every existing assertion for criteria 1, 2, 3, 5 and 6 stays exactly as it is: w10's `readStep('i1','research:i1.0','plan')` check (test-repo.sh:1734), the cross-mode `MARKER-PLAN-TEXT` loop (1418-1421), the cross-mode fields loop (1379-1385), w37's `!prompt.includes('research:i1')` check (2286) and its label-sequence assertion, w38's findings-read check (2301) and its label-sequence assertion, and the two `agents/researcher.md` shell checks.\n\nFOR THE IMPLEMENTER\n\nExpect nothing to build. The test-author's assertions pass against the tree as it stands, because the finding is a missing case and not a broken behaviour. Do not edit `workflows/agile-loop.js`, `agents/researcher.md` or any other production file to make something pass, and do not add or relax an assertion. Confirm the working tree carries no production diff, run the check list, and report the exit code.\n\nDESIGNS CONSIDERED AND REJECTED\n\n- Make `readStep` throw when the label is empty. It would catch the same break (the driver run would crash) and would defend every call site, but it turns a rendering slip into a crashed run in production, and the criterion asks the prompt to stay valid, not the script to abort. The assertion catches the same mutation at no production risk.\n- Keep the narrow `i<n> research:` regexes and add a third assertion per mode. Rejected: the plan read is the only research-step read a researcher prompt carries, so `--fields plan` subsumes the narrow form, and a third assertion is a line of cost that catches nothing new.\n- Snapshot the whole correction-round prompt and compare. Rejected: it re-fails on every unrelated wording change to a prompt and pins nothing about this criterion.\n- Put the empty-label check inside w37 and w38 only. Rejected: the same slip can hit the tests, implement and review reads (`previousTestsLabel`, `testsLabel`, `verdictLabel` are all guarded the same way), and the cross-mode block is where the driver already keeps rules of exactly this kind.",
                "moduleMap": [
                  "/home/user/uroboros/test-repo.sh — the repository's own suite: bash checks over the agent pages and the skills, plus an embedded Node driver (heredoc from line 834) that loads `workflows/agile-loop.js` with a stubbed `agent` and asserts what every dispatch's prompt says. THE ONLY FILE THIS ROUND CHANGES. Entry points: `assertTrue`/`assertEqualArrays`/`fail` (841-843); `STATE_PATH = 'docs/issues/x/backlog.json'` (887) and the `readStep(id, label, fields)` renderer (890) that mirrors the workflow's own; `contextFor(mode)` fixtures, w37 at 1312 and w38 at 1323; `main()` (1354) with the cross-mode 'checked in every mode' assertions at 1373-1422 — the new validity check belongs there; the per-mode assertion chain, `mode === 'w37'` at 2275 (its plan-read assertion at 2288) and `mode === 'w38'` at 2290 (its plan-read assertion at 2300); `run_driver` and the mode registrations at 2366-2367; the researcher-page checks for criterion 5 at 594-609.",
                  "/home/user/uroboros/workflows/agile-loop.js — the agile loop: it builds every agent prompt and dispatches the steps. NOT CHANGED THIS ROUND; it is the mutation target the new cases are measured against. Entry points: `readStep(incrementId, label, fields, what)` (624) and `readBlock(intro, lines)` (634), the renderers of every state read; the researcher dispatch's correction-round `readBlock` with the guarded plan read (1155-1180), whose `previousPlanLabel ? ... : ''` conditional is break #4; `planLabel`/`previousPlanLabel` declared per increment (1103-1104) and rolled at the top of the round loop (1110-1111); `planLabel = ''` in the direct/direct-fix branch (1135-1136); the closing planner's whole-increment read (1402), the one legitimate `steps` line that names no step label.",
                  "/home/user/uroboros/agents/researcher.md — the researcher's page, criterion 5's target. NOT CHANGED THIS ROUND. Entry points: the frontmatter `description:` line (3) with its 'the plan the round before wrote' clause, and the '## Correction rounds' section (119-135) whose sentence at 128-131 tells a correction round's researcher to read the round-before's `plan` first and what to do when no such step is named.",
                  "/home/user/uroboros/test.sh — the aggregate suite behind `bash test.sh`; runs `test-repo.sh` first, then the worktree suite, the backlog recorder, the two hook suites and the three npm tool suites. NOT CHANGED. Entry point: the `run` lines, one per suite."
                ],
                "environment": [
                  "`bash test-repo.sh` — run from the repository root. The single-file command for this round's cases: the workflow driver is generated into a temp directory by that script, so there is no narrower way to run the w37/w38 cases. Needs `bash` and `node` on PATH; nothing to install.",
                  "`bash test.sh` — run from the repository root. The one check this increment is judged by; it runs `test-repo.sh` first and seven further suites. The npm tool suites it calls are zero-dependency, so no install step is needed.",
                  "`git checkout -- workflows/agile-loop.js` — reverts the temporary mutation the test plan asks the test-author to apply while proving the break. Run from the repository root.",
                  "There is no linter and no formatter in this repository, and no build step."
                ],
                "testPlan": "WHETHER: tests are needed. The review's finding is that a named break leaves the suite green, so this round is entirely about the cases that close that gap.\n\nWHAT IS ALREADY RED: nothing was run for this plan, and the list below is unrun. No decision here turns on a fact only a run can settle: the reviewer's reproduction already recorded the exact line the broken build renders (`  - `steps docs/issues/x/backlog.json i1  --fields plan` — the plan you wrote for the round before: ...`) and the exit code of every named break, and the one open risk — whether a cross-mode empty-label check could false-positive on a legitimate read — I settled by search, not by running: `readStep` (workflows/agile-loop.js:624) and the closing planner's line (1402) are the only two renderers of a `steps` read in the workflow, and 1402 is the reason the check skips `replan:` dispatches. The first run is yours.\n\nWHAT: three cases, all for criterion 4 — 'Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid'. Cases B and C replace existing assertions in place; case A is new.\n\nA. Criterion 4, the 'stays valid' half, in every mode — the driver runs `workflows/agile-loop.js` with the stubbed `agent` in each of its modes and collects every dispatched prompt → for every call whose label does not start with `replan:`, every prompt line containing `steps ` + `STATE_PATH` names a non-empty step label: after the state path comes the increment id and then a step label made of non-space characters. A line where the increment id is followed by a second space (the shape an empty label renders when fields are named) or by the closing backtick (the shape it renders when they are not) fails the assertion, and the message carries the call's label and the offending line. `replan:` is skipped, and the assertion's comment says why: the closing planner reads its increment whole and names no step (workflows/agile-loop.js:1402), which is the one correct labelless read in the workflow. Place it in `main()`'s 'checked in every mode' block (test-repo.sh:1373-1422), beside the existing 'reads a step without naming the fields it may see' loop; it may share that loop's iteration as long as it keeps its own message. — break: replace the `previousPlanLabel ? readStep(task.id, previousPlanLabel, 'plan', ...) : ''` entry at workflows/agile-loop.js:1162-1170 with an unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)`; w37's `research:i2.1` and w38's `research:i1.2` then render a read with an empty step label and this assertion fails in both modes.\n\nB. Criterion 4 in w37 — i2 is the planner's direct increment whose round-0 review files one non-direct finding, so `research:i2.1` is a full correction round whose own previous round dispatched no researcher → `research:i2.1`'s prompt carries no plan read at all: it contains no `--fields plan`. This replaces the existing assertion at test-repo.sh:2288, whose regex `/steps docs\\/issues\\/x\\/backlog\\.json i2 research:/` is what the empty label slips through; the message stays about i2's correction round being sent to a plan although i2's own round 0 dispatched no researcher. Leave w37's other two assertions (the label sequence at 2280-2283 and the `!prompt.includes('research:i1')` check at 2286) exactly as they are. — break: the same unconditional plan read as in case A; also deleting `planLabel = ''` from the direct/direct-fix branch at workflows/agile-loop.js:1135-1136.\n\nC. Criterion 4 in w38 — one full increment whose round-0 review files an all-direct finding (round 1 is a direct-fix round with no researcher) and whose round-1 review files an ordinary finding, so `research:i1.2` is a full correction round after a round that dispatched no researcher → `research:i1.2`'s prompt carries no plan read at all: it contains no `--fields plan`. This replaces the existing assertion at test-repo.sh:2300; the message stays about round 2 being sent to a plan although the round before it dispatched no researcher. Leave w38's other two assertions (the label sequence at 2295-2298 and the findings-read check at 2301) exactly as they are. — break: the unconditional plan read of case A renders an empty-label plan read here too; and deleting `planLabel = ''` at workflows/agile-loop.js:1135-1136 makes this prompt name `research:i1.0`, the plan two rounds back — both now fail this assertion.\n\nWHAT IS DELIBERATELY NOT RE-TESTED: criteria 1, 2, 3, 5 and 6 are covered by cases that already sit in the tree from round 0, and this round neither rewrites nor removes them — w10's `readStep('i1','research:i1.0','plan')` assertion (test-repo.sh:1734) for criteria 1 and 6; the cross-mode `MARKER-PLAN-TEXT` loop (1418-1421) and the cross-mode 'reads a step without naming the fields' loop (1379-1385) for criterion 2; w37's `!prompt.includes('research:i1')` check (2286) for criterion 3; the two `agents/researcher.md` shell checks (594-609) for criterion 5. Add no case of your own for them and touch none of them.\n\nHOW: all three cases are unit-level assertions in the embedded Node driver of `/home/user/uroboros/test-repo.sh`, run by the file's own bash harness (`run_driver` at 2321, `ok`/`no` reporting, `assertTrue`/`assertEqualArrays` at 841-843) — no external framework. The command that runs just this file is `bash test-repo.sh` from the repository root; the driver is generated into a temp directory by that script, so there is no narrower command. Reuse `STATE_PATH` (887) and, where a whole read line is asserted, the driver's `readStep` renderer (890) rather than a hand-written string.\n\nSUITE DOC: there is no `CLAUDE.md` beside `test-repo.sh`, and README.md:123 is the only note about it anywhere in the repository. Write none for this round: the file sits at the repository root, so a doc beside it loads into every agent working this repository for the sake of one suite, and the conventions the new cases need are already legible in it (per-mode fixtures in `contextFor`, one `run_driver` line per mode, the assert helpers, the mirrored `readStep`). If your page leaves you unable to skip it, put that in `openQuestions` instead of writing it.\n\nPROVING THE BREAK: these three cases pass the moment you write them, because the production code is already correct and the finding is a coverage gap. So run `bash test-repo.sh` and confirm exit 0 first. Then prove the break by hand: edit `workflows/agile-loop.js` in the working tree, replacing the `previousPlanLabel ? readStep(...) : ''` entry at 1162-1170 with the unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)`; re-run `bash test-repo.sh` and confirm it exits non-zero with case A failing in w37 and w38 and cases B and C failing too; quote those failures in your return; then run `git checkout -- workflows/agile-loop.js` and re-run `bash test-repo.sh` to confirm exit 0 again. Commit no production change — the tree you commit must differ from HEAD in `test-repo.sh` alone.\n\nWHAT COUNTS AS DONE: `bash test.sh`, from the repository root. That is the whole list; run nothing else.",
                "checks": [
                  "bash test.sh"
                ],
                "breaks": [
                  "A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix — delete the guarded plan `readStep` entry from the researcher dispatch's readBlock at workflows/agile-loop.js:1162-1170: w10's `readStep('i1','research:i1.0','plan')` assertion (test-repo.sh:1734) fails.",
                  "The prompt names that step and its field and copies no plan text into itself — pass `''` as the fields argument of that plan `readStep` (the line then renders without `--fields`): the cross-mode 'reads a step without naming the fields it may see' loop (test-repo.sh:1379-1385) fails; paste the plan text into the prompt instead of naming the read: the cross-mode `MARKER-PLAN-TEXT` assertion (test-repo.sh:1418-1421) fails.",
                  "The step the prompt names belongs to the increment being worked and never to another increment — hoist `previousPlanLabel`/`planLabel` out of the per-increment scope (workflows/agile-loop.js:1103-1104) so they carry from one increment to the next: w37's `!round1.prompt.includes('research:i1')` assertion (test-repo.sh:2286) fails on `research:i2.1`.",
                  "Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid — replace the `previousPlanLabel ? readStep(...) : ''` conditional at workflows/agile-loop.js:1162-1170 with an unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)`: the new cross-mode empty-label assertion (case A) fails in w37 and w38, and the new no-`--fields plan` assertions in w37 (case B) and w38 (case C) fail as well; delete `planLabel = ''` from the direct/direct-fix branch at workflows/agile-loop.js:1135-1136: case C fails, since `research:i1.2` is then sent to the plan two rounds back.",
                  "Every page that describes what a correction round's researcher reads describes this too — delete the 'Where your prompt names the research step of the round before, read its `plan` first' sentence from '## Correction rounds' in agents/researcher.md, or revert the frontmatter description's correction-round clause to naming the reviewer's findings alone: the two shell checks at test-repo.sh:594-609 fail.",
                  "A test turns red when the prompt no longer names the previous round's plan — delete the plan `readStep` entry at workflows/agile-loop.js:1162-1170: w10's assertion at test-repo.sh:1734 fails, and `bash test-repo.sh` exits 1.",
                  "`bash test.sh` exits 0 — any of the breaks above: `test-repo.sh` exits 1 and `test.sh` reports the failed suite and exits 1."
                ],
                "unbreakable": [],
                "questions": [],
                "rulings": [
                  "Close the finding with test assertions alone and change no production code, because the reviewer's own probe showed the run renders the correct prompts and the gap is a missing case; wrong costs one further correction round if a production defect is hiding behind the missing assertion.",
                  "The empty-label validity check goes in the driver's cross-mode block and skips `replan:` dispatches, because the closing planner legitimately reads its increment whole with no step label (workflows/agile-loop.js:1402) and is the only such read in the workflow; wrong costs a false failure in every mode, visible on the first run and removable in a line.",
                  "Replace the narrow `i<n> research:` regexes in w37 and w38 rather than adding a third assertion beside them, because the plan read is the only research-step read a researcher prompt carries, so the no-`--fields plan` check subsumes them; wrong costs one lost assertion, re-addable in a line.",
                  "The test-author proves the break by applying the named mutation to the working tree, re-running and reverting it with `git checkout -- workflows/agile-loop.js` before committing, because these cases are regression guards that pass on arrival and its page still asks it to prove the failure; wrong costs a stray production diff in the commit, which the reviewer sees.",
                  "No suite doc is written for this round, because `test-repo.sh` sits at the repository root and a `CLAUDE.md` beside it would load into every agent working this repository for the sake of one suite; wrong costs a missing doc, cheap to add later."
                ],
                "summary": "Round 1 closes the reviewer's finding with test assertions only: a cross-mode driver check that no prompt renders a state read with an empty step label (skipping the closing planner, which reads its increment whole), and w37's and w38's plan-read assertions retightened to reject any `--fields plan` read at all. Production is already correct, so nothing in `workflows/agile-loop.js` or `agents/researcher.md` changes and `bash test.sh` is the one check."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json`, before you begin.\nThis is correction loop 1 of 2 for this increment, and what the round before produced is your work order. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 review:i1.0 --fields findings` — the review's findings.\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.0 --fields openQuestions` — what the test-author left open, and it is yours to settle.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `breaks`\n  - `unbreakable`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "tests:i1.1",
              "at": "2026-08-17T14:13:02.058Z",
              "return": {
                "cases": [
                  {
                    "case": "A. Criterion 4, the 'stays valid' half, in every mode — every dispatch (except `replan:`) whose prompt carries a `steps` read names a non-empty step label.",
                    "file": "test-repo.sh",
                    "testName": "docs/issues/2026-08-17-correction-rounds-read-the-plan, criterion 4 (the 'stays valid' half) — loop in main()'s 'checked in every mode' block, beside the 'reads a step without naming the fields it may see' loop",
                    "want": "For every dispatched call whose label does not start with `replan:`, every prompt line containing `steps docs/issues/x/backlog.json` names a non-empty step label after the increment id — no line where the id is followed by a second space (the empty-label shape when fields are named) or by the closing backtick (the empty-label shape when they are not).",
                    "got": "Passes against the unmodified `workflows/agile-loop.js`. Against the break (the unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)` replacing the `previousPlanLabel ? readStep(...) : ''` ternary at workflows/agile-loop.js:1162-1170), fails in w10, w37 and w38 with e.g.: `research:i1.1 reads a step with an empty step label: [\"  - `steps docs/issues/x/backlog.json i1  --fields plan` — the plan you wrote for the round before: correct that design instead of deriving it again.\"]` (w10 shown; w37/w38 fail with the analogous line for `i2` and round 2)."
                  },
                  {
                    "case": "B. Criterion 4 in w37 — i2's round 1 is a full correction round whose own previous round (i2's round-0 direct round) dispatched no researcher, so `research:i2.1`'s prompt carries no plan read at all: it contains no `--fields plan`.",
                    "file": "test-repo.sh",
                    "testName": "mode w37: docs/issues/2026-08-17-correction-rounds-read-the-plan, criterion 3 (negative) and criterion 4",
                    "want": "`research:i2.1`'s prompt does not include the substring `--fields plan`, since i2's own round 0 dispatched no researcher for round 1 to read a plan from.",
                    "got": "Passes against the unmodified `workflows/agile-loop.js`. Against the break, fails with: `research:i2.1 reads a step with an empty step label: [\"  - `steps docs/issues/x/backlog.json i2  --fields plan` — the plan you wrote for the round before: correct that design instead of deriving it again.\"]` and `i2's correction-round researcher prompt carries a plan read, though i2's own round 0 dispatched no researcher to have written one`."
                  },
                  {
                    "case": "C. Criterion 4 in w38 — round 2's own previous round (round 1, a direct-fix round) dispatched no researcher, so `research:i1.2`'s prompt carries no plan read at all: it contains no `--fields plan`.",
                    "file": "test-repo.sh",
                    "testName": "mode w38: docs/issues/2026-08-17-correction-rounds-read-the-plan, criterion 4",
                    "want": "`research:i1.2`'s prompt does not include the substring `--fields plan`, since the round before it (the direct-fix round) dispatched no researcher.",
                    "got": "Passes against the unmodified `workflows/agile-loop.js`. Against the break, fails with: `research:i1.2 reads a step with an empty step label: [\"  - `steps docs/issues/x/backlog.json i1  --fields plan` — the plan you wrote for the round before: correct that design instead of deriving it again.\"]` and `round 2's researcher prompt carries a plan read, though the round before it (the direct-fix round) dispatched no researcher to have written one`."
                  }
                ],
                "openQuestions": [],
                "questions": [],
                "rulings": [
                  "Wrote no CLAUDE.md beside test-repo.sh, because the plan's SUITE DOC section says one loads into every agent working the repository for the sake of one suite and the file's own conventions are already legible in it; wrong costs a follow-up commit adding the doc."
                ],
                "summary": "Added case A (an empty-step-label check for every non-`replan:` dispatch, in the 'checked in every mode' block) and replaced the w37 and w38 empty-label regex assertions (cases B and C) with substring checks for `--fields plan`, all three failing only on the plan's named break (the unconditional previous-round plan read) and passing again once it is reverted; `bash test.sh` exits 0."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.1 --fields testPlan` — the test plan.\nThis is correction round 1 of this increment.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "implement:i1.1",
              "at": "2026-08-17T14:15:48.892Z",
              "return": {
                "deviations": [],
                "commands": [
                  "`bash test.sh`, exit 0 — all 8 suites pass (test-repo.sh first, then the worktree suite, the backlog recorder, the two hook suites and the three npm tool suites; the last reported 23 tests, 0 fail). Nothing skipped or excluded.",
                  "`git diff --stat cc83b36..HEAD -- workflows/ agents/ skills/ hooks/ tools/`, exit 0 — empty output, confirming this round carries no production diff, as the plan required.",
                  "`git show --stat defb969`, exit 0 — the test-author's commit touches only `test-repo.sh` and `backlog.json`."
                ],
                "blockers": [],
                "questions": [],
                "rulings": [
                  "Ran no red-first check of the planned cases and applied no temporary mutation to `workflows/agile-loop.js`, because the plan states the finding is a coverage gap rather than a production defect and the test-author already proved the break against the mutation; wrong costs the reviewer re-running the plan's documented mutation (replacing the `previousPlanLabel ? readStep(...) : ''` ternary at agile-loop.js:1162-1170 with an unconditional read) to reconfirm the cases fail."
                ],
                "summary": "This round needed no code: the review's finding was a missing test case, and the test-author's cross-mode empty-step-label assertion plus the tightened w37/w38 `--fields plan` checks are already in `test-repo.sh`. Confirmed the working tree carries no production diff since cc83b36 and `bash test.sh` passes all 8 suites, exit 0."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 research:i1.1 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 tests:i1.1 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `bash test.sh`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 implement:i1.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 implement:i1.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            },
            {
              "label": "review:i1.1",
              "at": "2026-08-17T14:24:44.380Z",
              "return": {
                "findings": [],
                "findingCount": 0,
                "allDirect": true,
                "reason": "",
                "head": "0730a9677eefa59c41486e30fb1c07ce148ff508",
                "questions": [],
                "rulings": [],
                "summary": "The plan's second named break for criterion 4 (dropping the previousPlanLabel guard so the plan read renders with an empty step label) leaves bash test.sh green — addressed: the fix adds a cross-mode assertion rejecting any `steps <state> <increment> ...` read whose step label is empty, and re-points w37/w38 at the absence of a plan read; re-running that exact break in a sandbox worktree at 0730a96 now turns `bash test.sh` red (4 of 256 cases, exit 1), naming research:i1.1, research:i2.1 and research:i1.2 as reading a step with an empty label. `bash test.sh` in the unmodified checkout: 256 cases in the repo suite plus 7 further suites, all 8 green, exit 0. The two breaks criterion 4 names were applied one at a time in a sandbox worktree and both turn `bash test.sh` red — the unconditional plan readStep (4 cases, exit 1) and deleting `planLabel = ''` from the direct/direct-fix branch (1 case, w38's `--fields plan` assertion, exit 1). One probe, from the doubt whether the new loop's `replan:` exemption covers a real labelless read: deleting that line and running test-repo.sh in the sandbox left it green (exit 0), so the exemption is dead as the workflow stands — the closing planner's labelless read `steps <state> i1` does not match the loop's pattern anyway. Observation only: it exempts every replan prompt from the empty-label guard for no read that needs it. The fix touches test-repo.sh alone, no production code and no page, so nothing outside the tests changed; blast radius checked on the two assertions it rewrote — both still fail under the breaks their criterion names, and the wider `--fields plan` form also catches a plan read pointed at a step two rounds back."
              },
              "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nReview round 1 of increment 1. This is a correction round: the round before filed the findings below, this round was run to fix them, and that fix is what you judge.\nThe fix's diff is `git diff cc83b365c774959d19f915d70e33283ffccfe65d..HEAD` (two dots): everything committed since that review.\nThe findings that review filed, all of them:\n  1. The plan's second named break for criterion 4 — dropping the `previousPlanLabel` guard so the plan read renders with an empty step label — leaves `bash test.sh` green, so nothing verifies the 'stays valid' half of that criterion: a correction round after a round that dispatched no researcher can be sent an unrunnable read and no case catches it.\n     Reproduction: Doubt stated before probing: does any listed command notice a plan read that names no step? In a sandbox worktree at cc83b36, replace the `previousPlanLabel ? readStep(...) : ''` entry at workflows/agile-loop.js:1162-1170 with an unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)` (the plan's own second break variant for this criterion). `bash test.sh` exits 0 — 256 cases, 8 suites, all pass. Probe: the test-repo.sh driver was copied out of the sandbox and run in mode w38 with a dump of the `research:i1.2` prompt; it printed the line \"  - `steps docs/issues/x/backlog.json i1  --fields plan` — the plan you wrote for the round before: correct that design instead of deriving it again.\" — a read naming an empty step, which the researcher would run and fail on. The w37 and w38 assertions only reject a prompt matching /steps docs\\/issues\\/x\\/backlog\\.json i(1|2) research:/, so an empty label slips through both. The other five named breaks (delete the read entry, empty fields argument, module-scope lastPlanLabel, delete `planLabel = ''` from the direct branch, delete the researcher.md sentence, revert the frontmatter description) each turned test.sh red.\n     Criterion: Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\nThis run works docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md one increment at a time, and increment 1 is yours:\n  A correction round's researcher is sent to its own previous plan — A correction round's researcher prompt names the previous round's research step of the same increment and its plan field, and instructs the researcher to read it out of the run state before planning the fix; the pages that describe what such a researcher reads say so too, and a test turns red when the prompt stops naming it.\nWhat it has to satisfy:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix.\n  - The prompt names that step and its field and copies no plan text into itself.\n  - The step the prompt names belongs to the increment being worked and never to another increment.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid.\n  - Every page that describes what a correction round's researcher reads describes this too.\n  - A test turns red when the prompt no longer names the previous round's plan.\n  - `bash test.sh` exits 0.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe commands that count for this increment:\n  - `bash test.sh`\nThe break the plan named for each criterion it named one for:\n  - A correction round's researcher prompt names the research step of the round before for the same increment, and instructs the researcher to read that step's plan out of the run state before it plans the fix — delete the guarded plan `readStep` entry from the researcher dispatch's readBlock at workflows/agile-loop.js:1162-1170: w10's `readStep('i1','research:i1.0','plan')` assertion (test-repo.sh:1734) fails.\n  - The prompt names that step and its field and copies no plan text into itself — pass `''` as the fields argument of that plan `readStep` (the line then renders without `--fields`): the cross-mode 'reads a step without naming the fields it may see' loop (test-repo.sh:1379-1385) fails; paste the plan text into the prompt instead of naming the read: the cross-mode `MARKER-PLAN-TEXT` assertion (test-repo.sh:1418-1421) fails.\n  - The step the prompt names belongs to the increment being worked and never to another increment — hoist `previousPlanLabel`/`planLabel` out of the per-increment scope (workflows/agile-loop.js:1103-1104) so they carry from one increment to the next: w37's `!round1.prompt.includes('research:i1')` assertion (test-repo.sh:2286) fails on `research:i2.1`.\n  - Where the increment's previous round dispatched no researcher, the prompt names no such step and stays valid — replace the `previousPlanLabel ? readStep(...) : ''` conditional at workflows/agile-loop.js:1162-1170 with an unconditional `readStep(task.id, previousPlanLabel, 'plan', ...)`: the new cross-mode empty-label assertion (case A) fails in w37 and w38, and the new no-`--fields plan` assertions in w37 (case B) and w38 (case C) fail as well; delete `planLabel = ''` from the direct/direct-fix branch at workflows/agile-loop.js:1135-1136: case C fails, since `research:i1.2` is then sent to the plan two rounds back.\n  - Every page that describes what a correction round's researcher reads describes this too — delete the 'Where your prompt names the research step of the round before, read its `plan` first' sentence from '## Correction rounds' in agents/researcher.md, or revert the frontmatter description's correction-round clause to naming the reviewer's findings alone: the two shell checks at test-repo.sh:594-609 fail.\n  - A test turns red when the prompt no longer names the previous round's plan — delete the plan `readStep` entry at workflows/agile-loop.js:1162-1170: w10's assertion at test-repo.sh:1734 fails, and `bash test-repo.sh` exits 1.\n  - `bash test.sh` exits 0 — any of the breaks above: `test-repo.sh` exits 1 and `test.sh` reports the failed suite and exits 1.\nRead nothing out of docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 review:i1.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json i1 review:i1.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `head`\n  - `questions`\n  - `rulings`\n  - `summary`\n"
            }
          ]
        }
      ]
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-17T13:19:04.364Z",
        "return": {
          "questions": [],
          "rulings": [
            "Cut the issue as a single increment rather than splitting the workflow prompt from the pages and the test, because the criteria describe one change to one prompt and its two descriptions, and a split would leave the pages describing a read the prompt does not name yet; wrong costs one larger increment to review in a single chain instead of two smaller ones.",
            "Set the increment's depth to `full`, because the issue asks for a test that turns red when the prompt stops naming the previous round's plan and no such test exists; wrong costs a researcher and a test-author the run could have skipped."
          ],
          "summary": "Cut docs/issues/2026-08-17-correction-rounds-read-the-plan into one full-chain increment, i1, covering all seven acceptance criteria: A correction round's researcher prompt names the research step of the round before for the same increment -> i1; The prompt names that step and its field and copies no plan text -> i1; The step named belongs to the increment being worked -> i1; Where the previous round dispatched no researcher the prompt names no such step -> i1; Every page that describes what a correction round's researcher reads describes this too -> i1; A test turns red when the prompt no longer names the previous round's plan -> i1; `bash test.sh` exits 0 -> i1. The codemap names three files: the workflow that builds the prompt, the researcher's page that describes what a correction round reads, and the repository suite that holds the workflow driver cases. One increment because the criteria describe one change to one prompt plus its descriptions and its test; splitting it would land pages promising a read the prompt does not yet name."
        },
        "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nOpen the run state for docs/issues/2026-08-17-correction-rounds-read-the-plan/issue.md: map the issue against the codebase, decide the cut, and write both into docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json with the `init` subcommand of the backlog helper your shared brief names, with workflow \"agile-loop\" and the whole codemap in the payload's `codemap` field.\nThis run works at most 8 increments, so a cut that needs more than that is a cut that is too fine.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json - decompose <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json - decompose <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `rulings`\n  - `summary`\n"
      },
      {
        "label": "replan:i1",
        "at": "2026-08-17T14:25:36.190Z",
        "return": {
          "questions": [],
          "rulings": [],
          "summary": "Closed increment i1 as done after the review accepted it; the backlog holds no other open increment, so the cut and codemap stand unchanged and the run is complete."
        },
        "prompt": "Issue directory: docs/issues/2026-08-17-correction-rounds-read-the-plan\nIncrement i1 — A correction round's researcher is sent to its own previous plan — has been worked. The review accepted it.\nNothing else in the backlog is open, so there is nothing to re-cut: close this increment with `close`, leave the cut and the codemap as they stand, read nothing, and return an empty `increments` list.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json - replan:i1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/2026-08-17-correction-rounds-read-the-plan/backlog.json - replan:i1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `rulings`\n  - `summary`\n"
      }
    ],
    "attempts": []
  }
}
